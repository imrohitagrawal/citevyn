# Codex CLI Reference

## CLI flags

The --model flag selects the model Codex uses for code generation. Run
'codex --help' for the full list of flags.

## Errors

A "rate limit exceeded" error means the OpenAI_API_KEY environment
variable is set but the account is over its quota.
