# Claude Code Reference

## Permissions

Claude Code permissions are configured in the project's settings file.
Use the allow/deny lists to gate tools and commands the assistant can
run.

## Slash commands

Claude Code responds to /help, /compact, and /clear as built-in slash
commands.
