# Our promise

We don't use your questions, answers or feedback to train AI models. Feedback is used to find wrong or missing answers and fix the documentation index; it is not used to retrain any model.

# Our providers

Your questions are sent to the AI provider that writes each answer. Whether that provider may use them is set by its terms for the kind of account we use:

- Google Gemini (the main provider): [OWNER: confirm the API key is on a paid tier, where Google's terms say prompts are not used to improve its products, and link the clause. This is not yet verified (issue #492).]
- OpenRouter (the fallback): [OWNER: confirm the account's data policy setting, e.g. that providers which train on prompts are excluded, and link it.]

Until these are confirmed, this page is a draft and must not be published.

# What we keep

See the Privacy Policy for what we store and for how long.
