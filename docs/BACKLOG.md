# Backlog / open follow-ups

**Read this at the start of every work session, together with the live GitHub issue
list (`gh issue list --state open`).** This file is the durable, in-repo index of
tracked follow-up work so a session never re-implements or overlooks something that is
already filed. When you open, close, or supersede an issue, update the matching row here
in the same change.

> This file mirrors GitHub issues; GitHub is the source of truth for status. If a row
> here and the issue disagree, trust the issue and fix the row.

## Roadmap milestones

Post-MVP work is organized under two GitHub milestones (see `RELEASE_PLAN.md` §11–12):

- **[V1](https://github.com/imrohitagrawal/citevyn/milestone/1)** — depth/polish for a
  portfolio-grade demo (no new content domains or heavy surfaces).
- **[V2](https://github.com/imrohitagrawal/citevyn/milestone/2)** — breadth + heavier
  surfaces, deferred until V1 depth is proven.

### V1 milestone

| Issue | Title | Area | Notes |
|---|---|---|---|
| [#286](https://github.com/imrohitagrawal/citevyn/issues/286) | SQLite test suite has FK enforcement off — masks real Postgres-only bugs | backend / testing infra | Found live-testing ADR-0004 PR 12 (OAuth) against real Postgres: `_mint_principal` inserted a User + AuthSession together in one flush with no explicit ORM `relationship()`, so Postgres could reject the insert order — a bug invisible to 1500+ SQLite tests since PR 3 (SQLite FK enforcement was never on). That specific bug is FIXED (PR 12's branch) + proven via a dedicated `postgres`-marker regression test. Turning FK enforcement on globally (hook written + proven, then reverted to keep CI green) surfaces **140 more failures** — mostly (not all) unrealistic test fixtures using fake parent IDs, not confirmed additional production bugs. Owner decision: ship OAuth first, this is the next tracked item |
| [#153](https://github.com/imrohitagrawal/citevyn/issues/153) | Live hosted public demo + cost guardrails | infra / ops | Highest V1 ROI; also completes the Phase-5 live deploy-verify + rollback gate; §9 cost limits are a hard prerequisite before a public URL. **Layer 0 (provider cap) + Layer 6 (CI spend) DONE** — see `docs/COST_CONTROLS.md`; CI now runs the judged eval on main / `full-eval`-labelled PRs only, at full coverage (case sampling was measured and rejected: 43/60 golden cases carry a zero-tolerance oracle). **Layers 1, 2, 3, 5 DONE** (PRs #184/#188/#189/#196 — metering incl. the embedder seam, admission control, §9 daily budget, `GET /v1/admin/budget` + `make budget`). **Layer 4 (persisted per-user limiter) CLOSED as won't-build — see #197 below.** All six layers are therefore resolved |
| [#61](https://github.com/imrohitagrawal/citevyn/issues/61) | Real SSE streaming for chat answers | frontend / API | Verified: **no streaming route exists on `main`** — a real backend build, not a rewire |
| [#154](https://github.com/imrohitagrawal/citevyn/issues/154) | Feedback capture wired into the eval loop | backend / frontend | Value is the eval flywheel, **not** model retraining; most invasive V1 item (DB + API) |
| [#155](https://github.com/imrohitagrawal/citevyn/issues/155) | Evaluation + live-ops dashboard | frontend / observability | Surfaces existing eval metrics + live cost/latency/refusal; pairs with #154 |
| [#156](https://github.com/imrohitagrawal/citevyn/issues/156) | Better re-ranking of retrieved chunks | backend / RAG | Feature-flagged, cost-aware, proven on golden + distractor eval sets |
| [#62](https://github.com/imrohitagrawal/citevyn/issues/62) | Composer gating while a live answer is in flight | frontend | Small hardening; do alongside #61 |

### V2 milestone

| Issue | Title | Area | Notes |
|---|---|---|---|
| [#157](https://github.com/imrohitagrawal/citevyn/issues/157) | ChatGPT (OpenAI) official docs — 5th domain | backend / corpus | Deferred: breadth-not-depth **and** licensing-gated (ADR-0003). Not deferred for UI risk |
| [#158](https://github.com/imrohitagrawal/citevyn/issues/158) | Voice output (TTS) for answers | frontend / API | Large surface, off-core; explicit MVP non-goal |

## Open follow-ups (unmilestoned)

| Issue | Title | Area | Priority | Origin |
|---|---|---|---|---|
| [#288](https://github.com/imrohitagrawal/citevyn/issues/288) | `GET /v1/auth/me` reports an OAuth-created account with no verified provider email as `anonymous: true` (it keys on `email is None`, not the `usr_` prefix), so `AccountMenu` shows "Sign in" to a really-signed-in user who then cannot reach History / Connected accounts. Fix: derive `anonymous` from the principal prefix + a header fallback label from `providers` | backend / frontend / auth | Low (rare: needs an unverified provider email) | found reading `_auth_user_payload` during ADR-0004 PR 13 |
| [#289](https://github.com/imrohitagrawal/citevyn/issues/289) | OAuth env vars (PR 12's five + PR 13's `CITEVYN_OAUTH_CONNECT_MAX_SESSION_AGE_SECONDS`) appear in no env example, README env table, or DEPLOY_FLY secrets list — an operator following DEPLOY_FLY verbatim gets dead Connect/Continue buttons with no runbook trail (everything fails closed; docs-only) | docs / ops | Low | ADR-0004 PR 13 review, docs-contract lens |
| [#290](https://github.com/imrohitagrawal/citevyn/issues/290) | `HistoryDrawer` never takes focus on open (the opening menuitem unmounts with the menu, so Tab walks the page behind the backdrop) — the exact gap PR 13 fixed in `ConnectedAccountsDrawer`; 2-line mirror fix + strengthened focus test | frontend / a11y | Low | ADR-0004 PR 13 review, frontend lens |
| [#300](https://github.com/imrohitagrawal/citevyn/issues/300) | "Who are you?" is refused: self-referential questions do not reach the About CiteVyn source | backend / routing | Medium — core demo UX | Owner production QA 2026-09-03. **FIXED — PR #305 MERGED to `main` (`4b1c5e0`), post-merge CI green, branch deleted. NOT deployed** (owner batched one `fly deploy` for after all four QA items). `canonicalize_self_reference` (`app/guardrails/domain.py`) rewrites a closed, whole-message-anchored list of second-person phrasings to the CiteVyn question each one means (3 canonical targets: identity / capability / coverage), applied in `Orchestrator.ask` **before** conversation memory so a mid-session "who are you?" cannot inherit the previous turn's topic, and mirrored in the eval harness's `_retrieve_sources` (which re-implements the query pipeline — omitting it there would let the gate stay green if `ask` dropped the rewrite). The end anchor is load-bearing: "who are the Codex maintainers?" keeps `codex`, "what can you do with the Gemini API?" keeps `gemini_api`. Golden set gained `citevyn_lit_selfref_identity` plus `codex_lit_selfref_negative` (the echo-oracle negative — it goes red if the rewrite ever over-matches); the capability and coverage groups are NOT declared as golden cases because the hermetic seed carries only the Pro/membership chunk and the keyword arm needs two matching tokens on a multi-token query, so the harness cannot actually prove them — they are covered by unit + orchestrator tests instead. Growing the golden set 58→60 tripped the deliberate drift-detector `test_the_documented_saving_ceiling_is_still_true`; the judged-subset priority pool is now 43/60 and the documented ~28% sampling ceiling still holds (17/60 = 28.3%), re-decided rather than drifted. `docs/API_SPEC.md` gained a "Self-Referential Questions" section. T2 review (5 read-only lenses x 3 default-refute skeptics, 71 agents): 22 findings raised, 42 of 65 verdicts refuted, **0 surviving CRITICAL_BLOCKER**. Fixed from review: the anchor tail `\s*[?.!]*\s*$` backtracked QUADRATICALLY (63 ms of blocked event loop for one 4000-char message, the API cap, vs ~0.5 us for a real question) — collapsed to the single class `[\s,.!?]*$`, measured 62.9 ms -> 0.017 ms, with a ratio-based linearity regression test; curly/modifier apostrophes now match (`what’s your name` — the owner DICTATES, so the smart quote is the common form); a closed opener set (`hi|hey|hello|ok|okay|so|well|um`) admits "hey, who are you?", which was still hitting the refusal this issue is about; `docs/API_SPEC.md` named a route that does not exist (`GET /v1/sessions/{id}/messages` -> `GET /v1/sessions/{session_id}`); six stale 58/42 counts left over from the golden-set growth were corrected against MEASURED values (60 cases, 16 postgres_only, 44 judged, 25 reaching the LLM, 43 priority); and a test docstring claiming the ordering mutation turned it red was corrected. **Round 2 (3 skeptics on the fix diff alone, per AGENTS.md) caught the fix introducing its OWN defect:** adding `so` to the opener set collided with `_ELLIPSIS_RE` in `app/answer/memory.py`, which independently lists `so` as an elliptical continuation — so `is_anaphoric_followup("so what do you cover?")` is True and the round-1 correction ("no listed phrasing is anaphoric, the ordering is DEFENSIVE") was itself false. The before-memory ordering is in fact LOAD-BEARING: reversed, "so what do you cover?" becomes "What is Claude Code? so what do you cover?" and routes `claude_code` (verified). Kept `so` (a common opener), corrected both texts, and added `test_self_reference_survives_an_elliptical_opener_mid_session` — mutation-confirmed as the ONLY test that goes red on a true reorder. The linearity test was also hardened (1000-vs-4000 sizes so the threshold sits 2x clear of both linear and quadratic rather than at the 3.0 boundary where a contended machine measured 3.09 and 6.33, plus a partner assertion proving the timing probe is still a LISTED phrasing — without it the test degrades silently to a ~1.00 no-op ratio). **Advisory recorded, deliberately NOT fixed (would widen conversation-memory semantics beyond this issue): a self-referential turn cannot serve as a memory antecedent** — `recent_user_questions` replays the ORIGINAL "who are you?", which still classifies `unsupported`, so a following "what does it cost?" reaches back past it to an older product topic. Only reachable now that these questions answer at all. **Rounds 3-4 (the dimensions rounds 1-2 never commissioned: security, architecture/taste, performance, coverage, silent-failure, data-contract, completeness — 71 agents, 21 findings, 1 survived).** Security found NO new vulnerability (prompt-injection via the rewrite, cache cross-serving, guardrail bypass and Unicode case-folding evasion all probed clean). Two real gaps fixed: (a) **`app/worker/promotion_eval.py` — the PRODUCTION gate that writes the `EvaluationRun` row `promote_version` checks — is a third hand-kept copy of `ask`'s pre-routing pipeline, and it had silently drifted**; its docstring promises the number "measures the system production actually serves", which the #300 rewrite made false. 0 of 15 promotion cases are affected today (measured), so no current number moved, but a future self-referential case would have been silently under-measured. (b) the stale-count sweep had missed `.github/workflows/ci.yml` (4 sites) because the grep covered only `docs/` and `backend/tests/` — and the drift detector's own failure message named only 2 files, so it could not route anyone there; it now names all 4 including that ci.yml is outside any docs/tests grep. Coverage measured ephemerally (**this repo has NO coverage tooling installed or in CI**): domain.py 97%, orchestrator.py 91%, promotion_eval.py 97%; every line added by this branch is covered. Two findings the skeptics refuted but MEASUREMENT confirmed were fixed anyway, both being direct applications of the repo's own "if reverting the fix leaves it green, it is not a test" rule: the promotion_eval mirror line had **zero assertions** behind it (removing it left all 42 promotion tests green — coverage is not assertion), now guarded by two parity tests mutation-confirmed against both removal and order-swap; and 3 of the 8 discourse openers (`hello`, `okay`, `um`) had no positive assertion, since the opener-safety test asserts text is UNCHANGED and so stays green if an opener is dropped. **Recorded, not fixed:** no production signal that the rewrite fired; stacked openers ("ok so what can you do?") still refuse; the frontend offline/demo path has no #300 mirror (live path is unaffected — production runs `VITE_API_LIVE=true`); and `ask()` is now 518 lines carrying five spellings of "the question" (argues for reshaping beyond this issue). **Round 5 (the review-fixes themselves, which no round had seen — 42 agents, 13 findings, all ADVISORY_DEBT, 0 blockers).** Two defects in what had just been written: the promotion-mirror guard asserted on `inspect.getsource()`, so it PASSED a behaviour-breaking edit (compute the rewrite, assign it to an unused name, keep using the raw question — both call names still present and in order) while FAILING harmless refactors including the shared-helper extraction the file itself asks for; and its "behavioural" half was a literal tautology (`expected = f(q); assert f(q) == expected`) that passes with both functions replaced by identity. Replaced with a retriever spy asserting the query actually issued — mutation-proven strictly better (catches the discard AND the removal, survives the rename). Also fixed: the drift detector's OWN file still said 42; its message named 4 of 5 sites; `ci.yml` called the $0.026 figure "measured" where COST_CONTROLS derives it by extrapolation; COST_CONTROLS' "Answer calls" column was undefined and now ships the exact reproduction command plus an honest note that it is an UPPER BOUND (the confidence gate can still decline after retrieval); and `promotion_eval`'s omission list named only conversation memory when it also omits the ambiguous-alias LLM check (both omissions make the gate measure a FLOOR, which is the safe direction for a promotion decision). A THIRD false claim of mine was caught and corrected the same way as the `so` one: the replacement docstring said the test turns red on a reorder — it does not, and cannot, because the two passes provably commute (order could only matter if alias canonicalization turned a question INTO a listed self-referential phrasing, and every listed phrasing is plain English with no alias token). No migration, no new setting, no wire-shape change. Backend 1720 passed / 20 skipped (was 1630; the same 6 pre-existing local-`.env` failures) |
| [#301](https://github.com/imrohitagrawal/citevyn/issues/301) | "Send link" can be clicked repeatedly: no client cooldown, server allows 5 links in seconds | backend / frontend / auth | Medium | Owner production QA 2026-09-03. **FIXED — branch `fix/301-magic-link-send-cooldown`; not yet merged, not deployed.** The limiter could not express this: every role had its own LIMIT but all shared one `window_seconds` (3600), so registering a `magic_link_interval` role with limit 1 would have meant 1 per HOUR — a lockout. Both limiters gained a `_windows` override dict + `window_for(role)`, defaulting to the limiter-wide window so every pre-existing role is byte-for-byte unchanged; the Redis Lua script needed NO change (it already takes the cutoff and TTL as arguments). `_settings_match` compares the new role's WINDOW, not its limit (fixed at 1 by definition) — comparing limits alone would let an operator change the cooldown and keep serving a stale cached limiter. Own bucket prefix (`mlinterval`), applied before the lookup on BOTH branches, so it leaks no account existence and a refused click cannot consume one of the five hourly sends. Statement-count parity untouched (the limiter issues no SQL). Frontend: `AuthModal` disables the button for 60 s with a `Sent. Request another in 1:00` countdown (the first label rendered; it ticks down from there) on a plain `setInterval` (mockable), keeps the `role=status` notice, and renders the 429 inline WITHOUT starting a countdown (the server's remaining window is unknown, so inventing one would over-wait). **Mutation testing found two REAL gaps that RED-first had not:** `check()` reading `self._window_seconds` instead of `window_for(role=...)` SURVIVED — every route test asserts "first accepted, second refused", which is equally true of a 1-per-HOUR lockout, so the cooldown could have been 60x too long undetected; and the SAME bug on the REDIS path survived all 12 Redis tests, which matters because Redis/Upstash is what PRODUCTION runs and the two limiters have separate `check` implementations. Both now pinned by controlled-clock tests (30 s -> refused, 61 s -> accepted) on each limiter, with a partner assertion that ordinary roles still count over the full hour. Five existing tests legitimately needed several links per address (hourly drain, one-live-token, same-origin signals, and the PR-15 test proving a thief cannot silence the sign-in notice by draining the request bucket) and now use `tests.conftest.clear_magic_link_interval`, which clears ONLY the interval bucket — two of them assert the hourly ceiling still trips at [202, 202, 429], so a blanket `reset_limiter()` would have destroyed what they measure while leaving them green. Frontend tests use `fireEvent`, not `userEvent`: userEvent does not work under vitest fake timers here (probed in isolation — default and narrowed `toFake`, with `delay: null` and `advanceTimers` wired, every combination hung to the 5 s timeout). Bundle: main chunk 63,496 B gzip vs the 63,500 line (+1 B over baseline; the countdown rode the lazy `AuthModal` chunk, 2,866 -> 3,014 B gzip). Live-verified on real Postgres: registered and unknown addresses both 202 then 429 with identical copy, only ONE email written, browser countdown ticking 1:00 -> 0:57 with the notice kept, and the 429 rendered inline with the button left enabled. Backend 1728 passed / 20 skipped; frontend 192 passed (was 186). New setting `CITEVYN_RATE_LIMIT_MAGIC_LINK_INTERVAL_SECONDS` (default 60) documented in `.env.example`, README §5 and `docs/API_SPEC.md` §4c. **T3 review (6 lenses incl. security told to break it, x3 default-refute skeptics — 84 agents, 26 findings, 0 CRITICAL_BLOCKER, 0 surviving REQUIRED_CONTRACT; security found nothing that survived).** Fixed from it: the client cooldown was a bare tick counter, so (a) it was not keyed to the ADDRESS it was started for — correcting a mistyped email left the button disabled for a minute against an untouched bucket, contradicting the code's own comment — and (b) it counted TICKS, so a backgrounded tab (browsers throttle `setInterval` to ~1/min) would hold the button long after the server would accept. Now a DEADLINE plus the address, with remaining time derived from the clock on every render. Three test gaps the review proved by mutation, each of which had left the whole suite green: the `mode === "magic-link"` guard (dropping it disabled the PASSWORD sign-in button for 60s), the `_settings_match` interval clause (deleting it left all 1728 backend tests green — pure decoration), and the Redis bucket TTL. Docs: the new bucket was missing from `docs/SECURITY_MODEL.md`'s canonical bucket inventory; `_email_bucket_key`'s docstring named 2 consumers when it now has 4; the README row referenced `CITEVYN_RATE_LIMIT_MAGIC_LINK_PER_HOUR`, which had no row of its own in that table. Two false claims of mine corrected: the BACKLOG quoted a countdown label the UI never renders first (`0:59` vs `1:00`), and the test comment asserting "userEvent does not work under fake timers" was WRONG — narrowing `toFake` to setInterval/clearInterval/Date (leaving `setTimeout` real, which is what userEvent's internal waits need) works, so the tests now use real `userEvent` instead of `fireEvent`. A review also caught that my find-replace had applied the interval bypass to 12 tests when only 2 needed it; reverted and re-applied surgically, so the count is genuinely 5. Bundle after the rewrite: main 63,497 B gzip (+2 B vs baseline, 3 B under the line); AuthModal lazy chunk 2,866 -> 3,104 B gzip. Backend 1730 passed; frontend 196 passed (was 186). No migration |
| [#302](https://github.com/imrohitagrawal/citevyn/issues/302) | Re-asking an answered question from a landing section does not scroll to the existing answer | frontend | Medium | Owner production QA 2026-09-03. **FIXED — PR #314 MERGED to `main` (`f928928`), post-merge CI green, branch deleted. NOT deployed.** The issue's diagnosis ("the scroll fires before `ChatView` has mounted, so it lands nowhere") is **not what happens.** Reproduced in a real browser: the scroll DOES fire and DOES reach the top, then is dragged straight back off. `ChatView`'s passive stick-to-bottom effect is keyed on `messages`, which the landing hook rebuilds into a fresh array on **every** render, and the landing hero's demo animation keeps streaming while the chat screen is up — so that effect re-pinned the list to the bottom every ~24 ms, measured by instrumenting `scrollTop` writes (476 -> 668 -> 764 -> 888 ...). The one-shot `flashExisting` scroll lost inside one frame. **This is also why the existing suite stayed green while production was broken:** in demo mode the re-render storm settles before the flash, so all FIVE landing entry points that submit a question (hero box, hero TRY chips, ticker/marquee pills, persona buttons, and Pricing "Get Pro") pass on `main` — the defect only reproduces on the LIVE path. Note the new regression test drives ONE of those five (the persona button); the other four are not separately covered. (The issue also names the "Live demo" chips, which do not enter chat at all: `demoQuestions[].select` only swaps the in-place demo answer.) Fix: `ChatView` owns the scroll in a `useLayoutEffect` (the frontend's first) keyed on the highlighted bubble's **domId string**, not on `messages` — depending on the array re-issues the smooth scroll every frame so it never settles (mutation-proven). The load-bearing half is stopping the fight: the layout effect disarms the stick latch, and a `highlightHoldRef` blocks the scroll listener from RE-arming it (never from disarming it, so a reader who scrolls away still takes control) — because a smooth scroll away from the bottom *starts* at the bottom, so its first frames still read "at bottom" and re-armed the latch that had just been disarmed to run them. `flashExisting` is now state-only. Second half of the owner's fix shape: the chat composer takes focus on mount and unsent hero text is carried into it. **Interpretation the owner should sanity-check:** the clause "once a conversation exists, the landing entry points should hand over to the chat ... rather than act as a second entry point" was read as SATISFIED by composer focus + carry-across, with every entry point still SUBMITTING its question. The strict reading (entry points stop submitting once a conversation exists) was rejected because it makes this issue's own bug — re-asking from a landing section — unreachable, which would leave the other half of the same fix shape as dead code. The composer focus is also unconditional rather than gated on "once a conversation exists", because gating it would carry text into an unfocused box; the residual is that entering an EMPTY chat on mobile now raises the keyboard. **A test I wrote caught a real defect in my own fix**: `enterChat` closes over the pre-clear `state.heroInput`, so a hero SUBMIT copied the question the user had just asked back into the composer; `askHero` now opts out explicitly (`carryHeroText: false`) rather than the callback re-deriving provenance by string comparison. Two pre-existing defects found and fixed in the same function: each flash overwrote `timers.current.highlightTimeout` **without stopping it**, so a second flash inside the 2 s window was cut short (~1.1 s) by the first flash's timer; and the 10 ms restart timer was an untracked bare `setTimeout` that unmount never cleaned up. **12 mutations run sequentially, byte-copy restore, each confirmed applied before measuring; 11 killed, 1 survives.** Three were killed only after fixing the TESTS: the `[highlightedDomId]` dep, the timer-stop, and the highlight reset-to-restart line all initially SURVIVED. The reset one is the cautionary case — my first test for it **passed for the wrong reason**, because a pulse that had *ended* also read as "restarted" (absence mapped to -1, trivially less than the previous elapsed time); absence now maps to Infinity, and a direct probe established the real behaviour first (animation currentTime 917 ms -> 33 ms unmutated; 867 -> 933 -> 1050, i.e. no restart at all, mutated). The one SURVIVING mutation is recorded in the code itself: releasing `highlightHoldRef` in the effect cleanup is hygiene, not guarded behaviour — every explicit send re-arms the latch via `sendTick`, so within one mount nothing reachable distinguishes it. New tests: `ChatView.test.tsx` (7 — the **first unit test that renders `ChatView` at all**) + 4 Playwright. The scroll guard carries "live only" in its title so the live config's `grep` picks it up, but it runs in BOTH configs; the title says *the regression* is live-only, not the test. **T2 review, 7 read-only lenses (correctness, silent-failure, security, performance, architecture/taste, test-adequacy, docs+completeness); coverage skipped (no tooling — #308) and data/persistence skipped (nothing persisted, no wire change). 0 CRITICAL_BLOCKER; three REQUIRED_CONTRACT findings, all fixed and re-verified.** **(1) The headline fix did not actually work on remount.** `ChatView`'s `[sendTick]` effect was guarded with `sendTick === 0` ("initial mount, no send yet"), but `sendTick` lives in the hook's reducer and OUTLIVES the component — every landing->chat switch after an earlier send remounts with a non-zero tick, so that effect pinned to the bottom and RE-ARMED the latch immediately after the highlight effect had scrolled away and disarmed it. Reproduced in jsdom by instrumenting scroll ops: `sendTick=0` -> `["scrollTo(0)"]`, `sendTick=3` -> `["scrollTo(0)","scrollTop=5000"]` — #302 intact on exactly the mount-with-a-pending-highlight path the issue names. Now seeded from a ref so it fires on CHANGE, never on mount; three partner tests prove the mount pin, the send pin and the no-highlight pin all still work. **(2) My `useLayoutEffect` comment gave the wrong reason** ("places the list before paint" — false, the scroll is smooth). The real reason is effect-PHASE ordering: layout effects all run before passive ones, which is what puts `stickRef=false` ahead of the passive `[messages]` pin; as a plain `useEffect` declaration order wins and the pin fires first. The swap survived all 203 tests, so it is now pinned by a test asserting a highlight arriving with a fresh `messages` identity produces NO pin. **(3) One Playwright assertion was a tautology** — it exited via "Back to landing", which already cleared the hero box before this change, so deleting the carry's own clear stayed green; it now exits via a nav link (`goSection`), which does not clear. Also fixed from review: the hold-ref asymmetry (blocks only the RE-arm, never the disarm) was unguarded in BOTH directions and is now restructured into two named transitions plus a test pair; `prefers-reduced-motion` is honoured for the new smooth scrolls (the change made that violation newly EFFECTIVE — on `main` the scroll was overwritten within a frame, so a reduced-motion reader never saw the travel); `focus({preventScroll:true})`; and one cleanup path instead of two. **CI enforcement was the other REQUIRED_CONTRACT theme:** 3 of the 4 new Playwright guards run in NO CI job (#311), so the hook-level behaviours were moved into vitest, which CI does run — 6 new `ChatView` tests and 6 new `useLandingState` tests. **Second mutation round: 11 more mutations, all killed**, including four that had survived the first round (the `useLayoutEffect` hook choice, the hold asymmetry both ways, the `sendTick` guard, the untracked restart timer, the restart-timer stop, and the -1 reset). The only surviving mutation in the whole diff is the hold-ref release in the effect cleanup, recorded as hygiene in the code itself; `if (!el) return` is likewise near-unreachable and unguarded, recorded. Follow-ups filed rather than folded in: **#311** (demo Playwright suite broken on `main` and run by no CI job — since FIXED in PR #324), **#312** (the 40 Hz re-render storm that amplified this bug — a `useMemo` on `chatView` measurably takes idle chat-screen `scrollTop` writes from 221/12 s to 0 for +17 B), **#313** (`isSafeHref` accepts protocol-relative URLs; pre-existing). **Recorded, deliberately NOT fixed — the duplicate guard is silent to assistive technology.** The security lens found no introduced vulnerability (the `getElementById` argument has exactly one producer, `cv-msg-${i}`, unreachable by message content; the zero-`dangerouslySetInnerHTML` property still holds; focus cannot be stolen remotely since the effect is mount-only). The a11y lens found the real gap: a screen-reader user who re-asks an answered question gets NO new message, NO focus move, NO live region and NO toast — only a CSS pulse and a scroll they are not reading (WCAG 4.1.3 / 1.4.1). Pressing Enter again does nothing at all, since the composer is now empty. This is pre-existing behaviour, but this change WIDENS it two ways: the visual channel just went from broken to correct while the non-visual channel stayed at zero, and the new autofocus guarantees the user is sitting in the composer when the silence happens. The machinery is already imported into the same hook — `useToast` is destructured in `useLandingState` and `ToastHost` renders `aria-live="polite"` — so it is one `addToast` before the `flashExisting` return. Left out because announcing is a product-copy decision, not a scroll fix. Same lens: `<main data-screen-label="Chat">` has no accessible name, and answer text / refusal badges / source lists sit in no live region, so an AT user asks a question and hears nothing come back — a bigger gap than this issue, worth its own item. Also recorded: `if (!el) return` in the layout effect is near-unreachable and unguarded (mutating it survives), kept as defence. **A SECOND skeptic drove a real browser through eight break-it scenarios against `main` and the branch side by side, and reproduced three regressions I had introduced — all now fixed.** Its baseline is the best evidence in this row: with the duplicate sitting mid-conversation at `cv-msg-6` and a precondition of `d = -520` (fully off the top), on `main` the hero box, hero TRY chip, ticker pill and persona button ALL stayed at **-520 — never scrolled at all**; on the branch all five land at `d = +12` and hold there when sampled every 250 ms out to 6 s. Note the issue's list is slightly over-broad: **Pricing "Get Pro" was never broken on `main`** (it already landed at 12), so 4 of 5 entry points were affected, not 5. #122 stick-to-bottom measured byte-identical to `main`. The three regressions: **(F1)** returning to the true bottom INSIDE the 2 s hold no longer re-armed stick-to-bottom, and because re-arming is edge-triggered on a scroll event the miss OUTLIVED the hold (measured 97 px adrift at stream end vs 0 on `main`) — the hold now releases as soon as the list has genuinely left the bottom, which is the only window it was ever needed for. **(F2)** resuming a past session within 2 s of a flash opened it mid-conversation on an unrelated bubble (1004/2156 vs `main`'s 2156/2156) and **(F3)** re-entering the chat with no question opened at the very top (0/740 vs 740/740) — one pre-existing root cause the fix weaponised: `highlight` is an INDEX into `messages`, and neither `SET_SCREEN` nor `RESUME_SESSION` invalidated it. Both reducer cases now clear it, and a `stopFlash()` helper cancels the in-flight 10 ms restart timer on the three paths that leave or replace the list, since the reducer clear alone loses the race. F1 and F3 re-verified fixed in the browser (`{top:608,max:608}` and still following after a further stream). Eight more mutations, all killed — three of them only after the tests were corrected, since `SET_SCREEN`'s clear masked `RESUME_SESSION`'s, and the flash timers had already fired before the assertion. One line was DELETED rather than guarded (the `enterChat` timer stop: a ~10 ms window whose worst outcome is restoring a highlight belonging to the same message list). Verified live in a real browser on the final code: msg-0 starts 944 px off the top, lands at +8 px and STAYS there past the highlight window, composer focused, hero draft carried, duplicate guard held at 7 bubbles. **Skeptic round on the review-fix diff (per AGENTS.md) found three more guards passing for the WRONG reason — round 2 had claimed none were left.** The whole `[sendTick]` effect could be DELETED with all 215 tests green: the test guarding it never scrolled away first (so the latch was still armed from mount) and passed a FRESH `messages` identity, which fires the passive pin and writes the very same `scrollTop=5000` on its own — the effect contributed nothing to the assertion. `prefers-reduced-motion` was unguarded AND unguardable, because jsdom ships no `matchMedia`, so that branch was dead code in every unit test and every "smooth" assertion would have held with the preference set. And `expect(vi.getTimerCount()).toBeGreaterThan(0)` was vacuous — the hook always has its placeholder interval and hero loop pending, so it held even when the flash armed no timers (proved by stubbing `flashExisting` to arm none); now a delta. A fourth false claim of mine was corrected: the reduced-motion probe is NOT "the same probe `useRevealOnScroll` uses" — that file calls `matchMedia` UNGUARDED; the `?.` here is load-bearing, not defensive. The same skeptic independently CONFIRMED three claims: the scroll-listener restructure is exactly equivalent to the original (full truth table), `seenSendTickRef` is safe under StrictMode with nothing losing its scroll, and no prototype patch leaks between test files. Frontend 223 unit tests (was 196); demo Playwright unchanged at 39 pre-existing #311 failures, 0 new (diffed by `file:line` against `main`); live Playwright 3/3. Bundle: main chunk **63,816 B gzip vs a 63,497 B baseline (+319 B)** — **ceiling deliberately raised to 64.0 kB (64,000 B), recorded in the #270 row**, not squeezed; 184 B headroom left. Measured attribution: 174 B of the delta is the scroll fix itself and is not avoidable. `docs/UI_DESIGN.md` updated (composer, view switching, duplicate-question guard) with a changelog row, as that file requires. No backend change, no migration, no wire-shape change. |
| [#303](https://github.com/imrohitagrawal/citevyn/issues/303) | Answer rendering: raw markdown in replies, inert and unexplained `[n]` citation markers, duplicate cards per page | frontend / prompt | High — the product's core promise is invisible | Owner production QA 2026-09-03. **FIXED — PR #315 MERGED to `main` (`b805e80`), post-merge CI green, branch deleted. NOT deployed.** Backend is ONE prompt block (`app/llm/prompts.py`) constraining the model to `**bold**`, `` `code` `` and `- ` bullets and naming what it must NOT emit; the trailing "respond with exactly ... and nothing else" clause stays LAST and byte-exact, since `_is_no_answer_refusal` compares against it and `knowledgeBase.ts`'s `GENERIC_REFUSAL` mirrors it — pinned by a new `test_prompt_formatting_contract.py` asserting the ORDERING, not just the presence. Frontend: `lib/answerFormat.ts` parses that subset to **DATA, never an HTML string** (`parseAnswer` -> `Block[]`/`Span[]`), and `components/AnswerBody.tsx` maps it to elements. That shape IS the security design, not a detail: `dangerouslySetInnerHTML` appears zero times in this codebase, so every value lands in a text position React escapes and there is no escaper to get wrong — anything outside the subset is literal text BY CONSTRUCTION. The mandatory adversarial pass asserts it structurally (script tags, `img onerror`, `iframe`, `svg onload`, entity-encoded script and a `javascript:` markdown link all render as visible text, zero elements created, no global side effect), with a partner test proving the ALLOWED subset does produce real elements so those cases cannot pass vacuously. The parser runs on every streamed chunk, so half-written `**bold`, `` `code `` and `[1` stay literal until the closer arrives. Chips link to the source (new tab, `title` = source name) through `isSafeHref`, so an unsafe URL renders an INERT chip; a marker with no matching card renders as plain `[n]` (markers can be gapped and validation can drop a citation). Cards collapse by document — 3 citations across 2 pages become 2 cards, badge reading `1, 3` — which also resolves the `key={src.n}` collision collapsing would otherwise create, and needed a CSS fix since the badge was built as a fixed 22px square and wrapped `1,`/`3` onto two lines. Chip click highlights the card; card hover highlights **every chip OCCURRENCE** it backs. One-time-per-session legend under the first answer that actually renders chips, so demo mode (sources but zero markers) shows none. The reader's OWN text renders verbatim — no markdown, no chips — initially unguarded, now pinned. Copied text keeps the plain `[n]` form because the brackets are REAL characters inside the chip (dimmed via `.chip-bracket`), not CSS `content`, which a copy would drop. **16 mutations, all killed** (marker regex, empty-bold guard, unterminated-delimiter fallbacks, bullet regex, text coalescing, collapse key, numeric marker sort, miss path, `isSafeHref`, bracket characters, streaming gate, hover clear, chip click, legend once-only, legend demo suppression, user-text verbatim). **A live-browser assertion of MINE was wrong and the test caught it:** I expected 2 active chips when hovering a card backing markers 1 and 3, but `[1]` is written twice, so it is 3 chip OCCURRENCES — corrected, with a partner hover on the other card so it cannot pass as "all chips are always active". `docs/API_SPEC.md` gains an `answer` formatting contract (the subset, gapped markers, unmatched markers, shared URLs). No wire-shape change — `citations[].marker` already shipped in #258 — and no migration. **T2 review, three lenses (adversarial XSS, correctness+test-adequacy, a11y+design) plus a skeptic round on the review fixes. 0 CRITICAL_BLOCKER; the central security claim — "outside the subset is literal text BY CONSTRUCTION" — survived every attack (17 markup payloads produced 0 script/img/svg/iframe elements, 0 `on*` attributes, no global set). Ten real defects came out of it, all fixed.** **(1) Markers inside `**bold**` were swallowed** — `parseInline` captured bold as a raw string, so `**Rate limits: 30/hour [1]**` rendered the marker as inert punctuation, i.e. the exact defect this issue exists to fix, on output the SAME commit's prompt change actively encourages. Backend `_cited_markers` said `["1"]`, the client rendered no chip. Bold now holds SPANS and parses recursively; nesting is impossible by construction (the closer is the FIRST `**` after the opener), which a reviewer confirmed by brute-forcing ~10M inputs to max depth 1, so the `allowBold` parameter was DELETED rather than shipped unreachable. **(2) `isSafeHref` was a byte-identical COPY of ChatView's**, the function #313 already reports as broken — and this copy gates every chip on the SUCCESS path, not just declined-answer suggestions. Worse, **#313's own proposed fix is insufficient**: the WHATWG parser strips ASCII tab/LF/CR before parsing, so `/\t/evil.com` IS `//evil.com` and no `startsWith` patch catches it (measured: the obvious patch still returns true for the tab, LF and CR forms). Replaced both copies with one parser-based `lib/safeHref.ts`. A skeptic then found that version had **silently started ACCEPTING schemeless URLs** — `docs.claude.com/x` resolves against our origin and links to a same-origin 404 dressed as a doc link, and **all nine URLs in `knowledgeBase.ts` are that shape** (latent only because demo answers carry no markers). Now a shape guard admits exactly two forms: explicit http(s), or a `/`-rooted path that must stay on this origin. **(3) The scheme check is a deliberate unreachable backstop, and says so** — the shape guard refuses everything schemeless first (verified by enumeration), so no test can distinguish it; the skeptic caught my test comment claiming the opposite, and that my `blob:` fixture passed for the wrong reason (jsdom's base origin, not the guard). **(4) Marker numbering diverged from the backend** (unbounded `\d+` + `int()`, so `[01]` is marker 1) — aligned and normalised. **(5) The legend and the renderer disagreed**: the legend was gated on a regex over raw text, so a gapped or dropped citation showed a legend explaining chips that were not there; both now call one `hasCitationChips`, which a 20,000-case differential fuzz found zero disagreements for. **(6) A test asserted a chip is live mid-stream** — a state the app never produces, since `hasSources` is `!streaming && ...`; corrected, along with my commit message claiming the opposite. **A11y, all measured: (7) inline `<code>` had no break opportunity and was CLIPPED at mobile widths** — 41 px of characters simply gone at 390 px, a horizontal scroll at 320 px, on exactly the content the prompt asks for (WCAG 1.4.10); `overflow-wrap: anywhere`, re-measured to no overflow. **(8) The legend used `--faint`: 2.77:1 light / 3.49:1 dark at 11 px**, both failing AA — `--muted` gives 5.27:1 / 7.01:1; it is the one sentence explaining the feature. **(9) The legend said "below" while rendering BELOW the cards** — moved above them. **(10) The chip/card tie was mouse-only, and then the fix for it clobbered itself**: one shared `activeKey` meant moving the mouse off a card wiped the ring of a still-focused chip, and tabbing away cleared a card the mouse was still on (reproduced in Chromium, Firefox AND WebKit). Now separate focus/hover state with focus winning. Also: `aria-label` dropped from the inert chip (ARIA 1.2 prohibits it on role=generic, and a reader dropping it would announce a bare "2"), brackets no longer `aria-hidden`, and a dead `prefers-reduced-motion` block removed (neither selector declares a transition; `reset.css` already handles it globally). Collapsed-badge alignment: a shared `min-width` aligns the common cases — **a subgrid column aligned every width but cost the card its centred single-row layout; tried, measured, looked worse, reverted**, and the code says so. **41 mutations across three rounds, all killed** (2 only after a test was corrected). **A bundle disagreement was settled by measurement, not argument:** a reviewer reported 65,974 B and flagged mine as ~870 B low — the difference is `frontend/.env.local`, which a `git archive` checkout lacks, so it builds the NON-shipping variant (measured 66,089 B without `VITE_API_LIVE=true` vs 65,229 B with it, same tree; the Dockerfile sets it). Final **65,329 B gzip (+1,513 B over #302)**; a markdown library would have cost 8-12 kB, so hand-rolling paid for itself 5-8x, but the ceiling still moved 64.0 -> 66.0 kB (recorded in the #270 row, with the measurement caveat). **FOLLOW-UP, found by looking at the FIRST live answer after deploy (release v10):** the model wrote `* ` bullets, not `- `, so every bullet rendered a literal asterisk — the exact class of defect this issue was filed for, shipped by me. #303's own text said it ("the live model emits ... `*` bullets"); I implemented only the `- ` the fix-shape asked the PROMPT to request. The prompt CONSTRAINS the model, it does not compel it, so the renderer now accepts both markers and the prompt names both. The trailing-space requirement does all the disambiguation: `*notabullet` and `*emphasis* here` stay literal, and `**bold**` cannot match because the character after the first `*` is another `*`. An explicit bold-first guard was written, found UNREACHABLE by mutation, and deleted rather than shipped. 3 mutations, 2 killed, the third was the dead guard. Bundle 65,335 B (+6 B). Frontend 326 unit tests (was 223); backend 1734 (was 1730, +4); live Playwright 4/4; demo Playwright unchanged at 39 pre-existing #311 failures, 0 new. |
| [#312](https://github.com/imrohitagrawal/citevyn/issues/312) | Landing hero animation re-renders the whole app ~40x/second while the CHAT screen is displayed | frontend / correctness + code health | Medium — the amplifier that turned #302 into a production bug; **explicitly NOT a CPU emergency** | Found during the #302 review (2026-09-03), measured on `main`. `playHeroLoop` and `placeholderTimer` (`useLandingState.ts`) are never gated on `state.screen`, so the hero's `streamText` demo keeps dispatching while the landing DOM is unmounted; `chatView` is an unmemoized `.map`, so `ChatView` gets a fresh `messages` identity every time and its passive effect writes `scrollTop`. Instrumented in real Chromium: writes every ~24 ms with no user interaction (476 -> 668 -> 716 -> 764 -> 888 -> 926 -> 945); ~18/s sustained, bursting to 42/s. **A first framing of this as "pure waste" was corrected by measurement:** an A/B control (hero loop removed, 3 runs each at 1x and 6x CPU throttle) shows +33% React reconciliations but `LayoutCount` IDENTICAL in every pair (417/417/416 vs 417/417/417) and CPU within run-to-run noise — the extra renders emit no DOM change, and the whole loop costs ~0.5% of one core. The case is correctness and code health, not performance. #302 fixed the symptom at the right boundary (the highlight now HOLDS the list) but left the storm, so the next effect keyed on `messages` hits the same trap unprotected. Fix (measured): `useMemo` the `chatView` map on `[state.messages, state.highlight]` takes idle chat-screen `scrollTop` writes from 221/12 s to **0**, for **+17 B gzip** (of the 320 B headroom left under the new 64,000 B ceiling); AND pause the hero loop on `screen === "chat"` (the loop self-re-arms via `heroPause`, so the resume path needs its own test). Not done in #302 — it changes when effects fire across the whole chat surface. |
| [#313](https://github.com/imrohitagrawal/citevyn/issues/313) | `isSafeHref` accepts protocol-relative URLs (`//evil.com`), so an off-site link can render as an official doc citation | frontend / security | Low — corpus-gated, defence-in-depth | Found during the #302 review (2026-09-03); **pre-existing**, flagged because it lives in a file #302 touches. `ChatView.tsx`'s `url.startsWith("/")` clause is documented as "site-relative" but also admits `//evil.com/x` and `/\evil.com` (Chrome normalises `/\` to `//`), which resolve off-origin; a `docSuggestions[].url` of that shape renders as a clickable `<a target="_blank">` presented as an official doc link. `rel="noopener noreferrer"` is present, so no reverse-tabnabbing — the exposure is an off-site navigation dressed as a citation. Reachable only via ingested corpus URLs (admin-only today). Fix is one clause plus a test asserting BOTH directions — `//evil.com` rejected AND a plain `/about` still linked, since the About-CiteVyn source depends on the relative form. |
| [#316](https://github.com/imrohitagrawal/citevyn/issues/316) | The page loads a Fontshare stylesheet + preconnect for Satoshi, a typeface no CSS ever uses | frontend / performance / design | Low — one wasted render-blocking request, but it needs an owner decision | Found fixing #306 (2026-09-03). `index.html` preconnects `api.fontshare.com` and loads Satoshi at 4 weights; **both `grep` hits for "Satoshi" in the whole frontend are COMMENTS** — `--font-sans` and `--font-display` are `Geist`. Measured live: `body` computes to `Geist, system-ui, sans-serif`, `document.fonts.check("500 16px Satoshi")` is false, no Satoshi face in the loaded set. The font FILES are never fetched (a browser loads a webfont lazily and nothing matches), so the cost is the CSS round trip, not the 12 files. Two ways out and they are opposite: either Satoshi was meant to be the body font and the wiring was lost (`tokens.css:21` documents it as "clean body / UI sans", so the intent was real), or Geist is intended and the Fontshare link is dead weight — in which case the preconnect, the `<link>` and BOTH fontshare hosts should leave the CSP together, with `test_csp_covers_the_pages_real_origins.py` updated in the same change. Owner call. |
| [#322](https://github.com/imrohitagrawal/citevyn/issues/322) | CSP guards are last-wins while browsers are first-wins: a prepended directive passes every test and still executes | backend / security / test integrity | **High (guard integrity; no live vulnerability)** | Found reviewing #306/PR #317. Both parsers build the directive map with `directives[tokens[0]] = set(...)` (`test_security_headers.py:67`, `test_csp_covers_the_pages_real_origins.py:56`), so a repeated directive is **last-wins**; browsers use **first-wins** (CSP L3). Reproduced against the real `_CSP`: prepending `script-src 'unsafe-inline' https://evil` leaves the tests reading `{'self'}` while a browser reads `{'unsafe-inline', evil}` — **inline JS executes with every test green**; prepending `frame-ancestors *` likewise passes and also defeats `X-Frame-Options` (a present `frame-ancestors` supersedes it); appending `script-src-elem 'self' 'unsafe-inline'` passes because nothing asserts that directive. Separately, `base-uri`, `connect-src`, `form-action` and `img-src` have **no** equality assertion at all — `base-uri` re-enables `<base>`-tag script hijacking unnoticed. Fix: one byte-exact `assert _CSP == (...)`, keeping the per-directive assertions as documentation. The shipped policy is correct today (0 CSP violations in production after v11), so this is guard integrity, not an exploit. **FIXED — PR #327.** `test_security_headers.py` now pins the whole policy byte-exact (`_CSP == _EXPECTED_CSP`) as a BACKSTOP — the per-directive assertions stay, because they document WHY each origin is allowed and give the useful failure message, but a parser can only check the directives it is asked about. A partner test asserts no directive is repeated, with its own partner (`len(names) >= 9`) so "no duplicates" cannot pass on an empty list. **Both parsers now REFUSE a repeated directive** rather than silently resolving it last-wins. Mutation-proven with a clean before/after: each of the three bypasses was applied to the real `_CSP` and run against `origin/main`'s test file and this branch's — `script-src 'unsafe-inline' https://evil` prepended: **6 passed on main (survived) -> 5 failed here (killed)**; `frame-ancestors *` prepended: 6 passed -> 5 failed; `script-src-elem 'self' 'unsafe-inline'` appended: 6 passed -> 1 failed. Restored byte-identical, 8 passed. (A first attempt at this measurement was invalid — the shell mangled one mutant so it never applied, and the "before" arm still used the NEW parser; redone by checking out `main`'s test file for the baseline.) **An adversarial review then BROKE the first version of this fix, and it was the same mistake one layer over:** I pinned the CONSTANT, not the header the browser receives. Moving the very bypass this PR claimed to kill — appending `script-src-elem 'self' 'unsafe-inline'` — from `_CSP` to `apply_security_headers` left the **full backend suite green at 1755 passed**. Same for prepending `Script-Src 'unsafe-inline' https://evil` (first-wins in browsers, and the capitalisation also slips the duplicate check, since CSP directive names are case-insensitive but a dict is not) and for `base-uri *` / `connect-src *`, which no per-directive assertion covered. `_assert_common_headers` now asserts `headers["content-security-policy"] == _CSP` FIRST, so all three response paths (router, StaticFiles mount, unhandled-exception handler) are covered by one line no future directive can slip past. Re-mutation-proven: all three header-path mutants now fail 3 tests each, restored byte-identical, 8 passed. Honest limit, stated rather than implied: byte-equality against a literal in the test file is a mechanical guard against ACCIDENTAL weakening (which is what this issue is about) and only a review-visibility cost against a deliberate one — a mirrored two-place edit passes, but it has to surface in a diff to a file named `test_security_headers.py`. `test_csp_has_no_repeated_directive` is the part that cannot be satisfied by mirroring, since it derives from `_CSP` alone. No app code changed: `_CSP` itself is untouched, so there is no behaviour change to deploy. Full backend suite 1756 passed / 0 failed (the 6 local failures are `backend/.env` interference — verified by moving it aside), pyright 0 errors, ruff clean. |
| [#323](https://github.com/imrohitagrawal/citevyn/issues/323) | Bundle budget gate passes silently if the budget key is mistyped, and measures only one chunk of the eager graph | frontend / build / test integrity | Medium | Found reviewing #319. `scripts/check-bundle-size.mjs:39` reads `budget.eagerChunkGzipMaxBytes`; mistype that key and `max` is `undefined`, so `gz > undefined` is **`false`** and the gate prints `bundle budget OK` for a bundle of any size, with `headroom NaN B` the only symptom (verified in node). Fix: reject a non-positive-integer budget before comparing, with a test that mutates the key. Second, `:45-53` globs `index-*.js` and takes the largest — correct today (`vite.config.ts` sets neither `manualChunks` nor `entryFileNames`, verified) but coupled to Rollup's default output shape: adding `manualChunks` would move code into a `vendor-*.js` that stops counting, reporting headroom that does not exist. Deriving the eager set from `dist/index.html` (entry `<script type="module">` + every `modulepreload`) measures what the browser actually fetches before first paint. |
| [#325](https://github.com/imrohitagrawal/citevyn/issues/325) | Visual snapshot suite runs in no CI job (darwin-only baselines), and `how-it-works` has a ±1px unstable height | frontend / testing infra | Medium | Follow-up from PR #324. `tests/visual.spec.ts-snapshots/` holds only `*-chromium-darwin.png`, so `playwright.demo-ci.config.ts` excludes `visual.spec.ts` — on ubuntu every screenshot would fail as "snapshot missing". The 10 baselines refreshed in #324 were each inspected individually and measured pixel-identical to `main`'s render, but **no CI job keeps them that way**. Fix option 1 (the only one that makes them a gate): generate Linux baselines in the official `mcr.microsoft.com/playwright` container, commit `*-linux.png`, drop the exclusion. Separately `how-it-works` oscillates 733/734/735px — it converges inside the retry budget but is *always* retrying (2.0-2.6s vs ~1.1s), making it the most likely future flake. |
| [#326](https://github.com/imrohitagrawal/citevyn/issues/326) | **Owner action:** frontend CI checks (Playwright + bundle budget) are advisory, not in `main`'s required status checks | CI / branch protection | **High — owner-only** | Measured from the protection API: required contexts are exactly `Analyze (python)`, `CodeQL`, `alembic + postgres integration tests`, `pytest + lint`, `quality-gate / quality-gate`. The **entire `frontend` workflow is advisory** — 326 frontend unit tests, the bundle budget gate (#319), the new 124-test `Demo-mode Playwright` job (#324) and `Live-mode Playwright` can all go **red and still merge**. #311 made the suite RUN; only branch protection can make it GATE. Trap: both frontend workflows use `paths:` filters, so a required check that never runs on a docs-only PR leaves it permanently "Expected — waiting for status". Remedy is a skip-shim job sharing the same `name:` on the inverse filter, or converting to `paths-ignore:`. |
| [#311](https://github.com/imrohitagrawal/citevyn/issues/311) | Demo Playwright suite is broken on `main` (70 runs fail on one CSS class collision) and no CI job runs it | frontend / testing infra / CI | **High — the suite that should have caught #302 runs nowhere** | Found verifying #302 (2026-09-03) on unmodified `main` (`5c99690`). `AccountMenu.tsx:73` and `:168` reuse `className="theme-toggle"`, the class `Header.tsx:52`'s real theme toggle uses, so `locator(".theme-toggle")` resolves to 2 elements and every test touching it dies on a Playwright strict-mode violation. Measured: **70 failing test runs (39 distinct `file:line` specs across the `[light]`/`[dark]` projects), 100% of them this one root cause** — categorised from the JSON reporter, not eyeballed; a main-vs-branch diff keyed on `file:line` confirmed the #302 branch adds 0 new failures and fixes 0. Invisible because `grep -rn playwright .github/workflows/*.yml` matches ONLY `frontend-live-e2e.yml`, whose config filters to `grep: /live only/i` = **4 tests**; the other 142 of 146 run in no workflow at all. Started with ADR-0004 PR 8 (which added `AccountMenu`). **FIXED — PR #324.** Class renamed to `.account-button`, added to the three `landing.css` selector lists that carried the shared styling, so the rendered appearance is unchanged. Demo suite on the branch: **121 passed / 3 skipped / 0 failed of the 124 the CI job runs** (was 39 specs failing on `main`); the full local suite including the 22 darwin-only visual tests is 143 passed of 146. Both numbers are real — naming which is which, because an earlier version of this row quoted 143 as if it were the CI figure. The second half — the CI job — is the load-bearing one: new `demo-e2e` job in `frontend.yml` runs the suite on ubuntu via `playwright.demo-ci.config.ts`, which excludes `tests/visual.spec.ts` because those baselines are `-darwin` only and would fail as "snapshot missing" on a Linux runner. **A job that runs zero tests passes, and there are two ways for that to happen, so there are two guards — both of which a skeptic round then had to strengthen because each still carried a magic number.** *Selection:* the CI run must select exactly the default suite minus its visual tests (`ci_total == all_total - all_visual`), it must select no visual test, and the DEFAULT config must still select some — the last being the partner without which deleting `visual.spec.ts` outright would satisfy "zero visual tests" just as well. Each mutation-tested: excluding `landing.spec.ts` too (124 -> 108) FAILS the differential where the original `>=100` threshold passed it; dropping `testIgnore` trips the visual check (0 -> 22); moving `visual.spec.ts` away trips the partner (22 -> 0). *Execution:* `--list` COUNTS SKIPPED TESTS and Playwright exits 0 when every test skips, so selection alone proves nothing. The run emits JSON and the job asserts a full account — `expected + skipped + unexpected + flaky == selected`, `skipped == 3` (exactly the `if (!isLive)` guards), `unexpected == 0`, `flaky == 0`. The skeptic broke the first version of this by setting `expected=100, skipped=24`: it passed, i.e. **21 tests could silently stop asserting**. Seven fixtures now run against the step's real script extracted from the parsed YAML; all seven behave (healthy 121/3 -> exit 0; that bypass, all-skipped, one extra skip, flaky, unexpected, and counts that do not add up -> exit 1). **Two stale assertions surfaced once the suite could actually run**, and one of them is this issue's whole thesis in miniature: `.composer-box` reads `--ink`, not `--border-2`, because **#302 — my own change, merged eight commits earlier — focuses the composer on mount**, so `:focus-within` applies. A visual regression I introduced sat undetected because nothing ran this suite. The test now asserts BOTH states and polls, since the box carries `transition: border-color .15s` and an instantaneous read samples an interpolated colour matching neither token. The other: the FAQ count is 7, not 6 — ADR-0004 PR 7 added "Do I need an account?". **10 visual baselines regenerated — and BOTH of my first two characterisations of them were wrong.** I wrote "pure SIZE mismatches": false. Pixel-diffing the top-aligned overlap shows up to **51%** of pixels differing (personas-light 51.3%, pricing-light 40.0%, demo-dark 14.2%) — and Playwright had said so in its own output (`demo-dark: 111492 pixels (ratio 0.15) are different`). I read the size line and stopped. "Pure size mismatch" is precisely the framing under which a real regression gets blessed. **My evidence for "not caused by this branch" was also invalid:** I cited "a pristine `main` worktree fails 21 of 22 visual tests" — but on `main` every visual test dies in `beforeEach` on the `.theme-toggle` strict-mode violation, *before* `toHaveScreenshot` is ever called, so those failures say nothing whatever about baseline staleness. Two valid controls, run independently. Mine: `main` + **only** the class rename (no spec edits, no new baselines) reproduces the same 10 failures with byte-identical numbers and **zero** strict-mode failures. A reviewer's, which is stronger: patch ONLY `helpers.ts` on `main` (`.theme-toggle` -> `.first()`, test-side, changes no rendering), render against **main's own** baselines — the same 10 fail — then compare main's rendered output against this branch's **new** baselines: **all 10 pixel-identical, 0 differing pixels, identical dimensions.** That is conclusive: the branch changes no pixel in these sections. Structurally it must not — `.account-button` renders only in `Header`, a `<header>` sibling outside every screenshotted selector, and it shares one rule block with `.theme-toggle`. **Each of the 10 was then inspected individually, and two of them were freezing BUGS as correct:** personas-light (51% differing) had a browser-default **white** page canvas where the new one has the themed cream `--bg` — the `body` background bug the fidelity suite's own comment documents; and demo (+83px) had the intent tags jammed inline against the question text (the old baseline literally reads `What is Claude Code?USAGE`) where the new one has the PR #143 tag-spacing fix. The new baselines capture fixes the old ones had frozen as correct. Provenance, since the deltas deserve names: the baselines were last written **2026-07-10** (`5b35457`) and ~20 frontend commits have landed since, including all of ADR-0004. `8c35590` (ADR-0004 PR 7, "honest pricing/FAQ copy") accounts for **faq +66px** (one accordion row) and **pricing +20px**; `7a42ab5` (#143, the demo panel showing the SELECTED question above its answer) accounts for **demo +83px**. `personas -1px` and `how-it-works +1px` are sub-pixel rounding drift and are NOT attributed to a commit — said plainly rather than folded into the claim. One `ticker` failure during the refresh did not reproduce in 3 further runs and left its baseline byte-unchanged — intermittent, and the section is animated. Also added `lint:css` to the build job: it was defined in `package.json` and run by no workflow — the same defect as this issue, one directory over; it passes on `main`. **The job earned its keep on its FIRST run by finding a real app bug — and my first diagnosis of it was wrong.** `behavior.spec.ts:553` ("scrolling up during streaming holds position") went flaky. I called it a test race — the scroll clamping short because the list had not overflowed yet — and added a precondition. **That was refuted by my own new partner assertion:** `expect(scrolled).toBe(40)` PASSED in every failure while `distanceFromBottom` was exactly **0**. The scroll moved the full 40px and the view was then *re-pinned*. A skeptic round reached the same conclusion independently and reproduced it at ~14% locally (3 of 21 runs at `retries: 0`). **The real defect:** the stick-to-bottom latch is disarmed by the `scroll` EVENT, which browsers dispatch asynchronously. If a streamed chunk commits between the reader's scroll and that event, the passive `[messages]` effect still sees an armed latch and pins to the bottom — and the event then reads "at bottom" (because the pin just put it there) and RE-ARMS. The reader's scroll is erased without a trace. Systematic on slower hardware, which is why CI hit it twice out of two. **Fixed in `ChatView` by checking the DOM instead of trusting the latch**, with a jsdom test that reproduces the ordering deterministically (position moved, event not yet dispatched, chunk arrives) plus a partner proving stick-to-bottom still follows. **My first fix for THAT broke stick-to-bottom outright** — a naive `scrollTop !== lastPinned` disarmed whenever streamed content SHRANK, because the browser clamps `scrollTop` down on its own. Measured in a real browser rather than guessed: `scrollTop=30 pinned=30 sh=492` then `scrollTop=0 pinned=30 sh=462`. Since clamping only ever lowers `scrollTop` TO the new maximum, the two are separable — `list.scrollTop < min(pinned, maxTop) - 1` is a reader scroll, anything else is not. The jsdom `scrollTop` stub now clamps on write like a real browser, since the unclamped stub made this untestable. 328 unit tests, 121 demo specs at `retries: 0`, 65 behavior specs. **Known gap, stated rather than papered over: the `frontend` workflow is NOT in `main`'s required status checks, so neither this job nor the bundle budget can BLOCK a merge until the owner adds it** (and adding it has a path-filter trap — a required check that never runs on a docs-only PR blocks it forever). Linux visual baselines are a separate follow-up. |
| [#308](https://github.com/imrohitagrawal/citevyn/issues/308) | No coverage tooling: the repo cannot measure test coverage without an ad-hoc install | tooling / CI | Low — measurement gap, blocks nothing | Found during #300: `pytest-cov` is not a dependency, `coverage` is not importable, no CI job measures it, so coverage had to be taken with an ephemeral `uv run --with pytest-cov`. Deliberately NOT a coverage-percentage gate: the same session proved **coverage is not assertion** — a line in `promotion_eval.py` was 97%-covered AND executed by the suite, yet deleting it left all 42 promotion tests green. A percentage would not have caught that, and a high number actively misleads. Proposal is measurement only (dev dep + `make coverage` + REPORT, not gate, in CI), with the blocking condition stated up front per AGENTS.md: blocking only after a baseline is stable across three consecutive `main` runs, and then as "no decrease vs baseline", never an absolute floor. Explicit non-goal: it does not replace mutation testing — mutation proves a test BITES, coverage only shows what was EXECUTED, and where they disagree mutation wins. **DONE — branch `feat/308-coverage-tooling`; not yet merged.** `pytest-cov` is now a declared dev dependency (it was previously reachable only via an ad-hoc `uv run --with pytest-cov`, which is what this issue was filed about), plus `make coverage` and a REPORT step in the `pytest + lint` CI job that uploads `coverage.xml` as an artifact. First measurement: **96%, 5267 statements, 185 missed**, on a green suite (CI, and reproduced on a clean checkout). An earlier figure of 92% / 421 missed was recorded here and in the commit message; that was a LOCAL run where 6 tests fail for a pre-existing environment reason (a `backend/.env` holding real keys), so 236 lines were never reached — corrected, because #308's own blocking condition is "no DECREASE against a baseline" and anchoring it on a broken run would have been wrong from day one. `make coverage` deliberately mirrors `make test` exactly — same marker exclusion, same `env -u CITEVYN_DATABASE_URL`, same in-memory SQLite — because a different invocation would measure a different program and the number would describe a run nobody does. **The advisory-ness is itself pinned by tests**, per AGENTS.md's rule that a gate shipped advisory must state in the same commit what makes it blocking: `test_coverage_is_reported_not_gated.py` asserts CI really measures coverage on the merge-gating suite, that `make coverage` still mirrors `make test`, that `pytest-cov` is declared, and — the load-bearing one — that **no `--cov-fail-under` exists in ci.yml, the Makefile or pyproject**, so the report cannot quietly become a threshold without someone deleting a test whose docstring says what they are trading away. That check parses the CI YAML's `run` values and strips comment lines, because the FIRST version failed on its own explanatory comment mentioning the flag — a test that was its own false positive. **A 7-lens review then broke the central claim.** The guard checked that the literal string `--cov-fail-under` was absent from three files; a reviewer found **eight working ways to make coverage blocking with the guard fully green** — `[tool.coverage.report] fail_under` in pyproject, a `.coveragerc`, `setup.cfg`, `tox.ini`, a `pytest.ini` `addopts`, a `PYTEST_ADDOPTS` env var on the CI step, a separate `coverage report --fail-under` step, and splitting the flag across a Makefile line continuation so the literal never appears contiguously. Proven end to end: the guard passed while the run reported "Required test coverage of 99.0% not reached". The irony is exact — **this change exists because coverage measures execution rather than checking, and its own guard had the same shape: it checked that a STRING was absent, not that the BEHAVIOUR was.** Rewritten to resolve the EFFECTIVE config through `coverage.Coverage().config.fail_under`, which closes every config-file route at once including ones nobody has invented, plus parsed checks of pytest `addopts`, CI `run` AND `env` blocks, and line-continuation joining. **All 8 routes now caught**, each verified individually. Two further guards were decoration: `makefile.count(fragment) >= 2` passed with `env -u CITEVYN_DATABASE_URL` deleted from the coverage recipe (it occurs 3x file-wide, so an unrelated target met the threshold) — now scoped to the extracted recipe; and the `pytest-cov` dep check was a whole-file substring that would pass with the dependency commented out — now parses the `dev` group. The upload step was untested and is now pinned to the path pytest writes. **A fabricated citation was also caught and removed:** four tracked files attributed the advisory-gate rule to `AGENTS.md`, which does not contain it — it lives in the owner's private global config, invisible to contributors. Also fixed: `if: always()` -> `!cancelled()`, `if-no-files-found: warn` declared rather than inherited, `make clean` now removes coverage residue, README's "Test coverage | 361 passed" row (5x stale, in a coverage commit) corrected to 1760 tests / 96%, and README §12 + `docs/TEST_STRATEGY.md` §4 now carry the policy — the latter being the document README names as the test-strategy source of truth, where a YAML comment was never going to be found. **Two findings survived that first fix and were closed after a fourth lens:** coverage could measure NOTHING and CI stay green — rename `app`, or add `[tool.coverage.run] omit = ["app/*"]`, and pytest exits 0, no XML is written, the upload warns, job green, every flag-checking test still passing; and the report had **no reader** (nothing consumed the XML, no job summary exists anywhere in the repo, so the number lived at the bottom of ~1760 `-v` lines in a collapsed log plus a 14-day artifact — present, but not visible). One step closes both: it fails the job when 0 lines were measured and writes the total to `$GITHUB_STEP_SUMMARY`. It asserts lines were MEASURED, never how many are covered — that distinction is what keeps it from being the gate #308 refuses. Its own guard **executes** the embedded script against a missing report, a zero-line report and a valid one, rather than grepping it for `sys.exit` — because asserting a string is present is the exact mistake this file was rewritten to stop making, and a partial edit leaving one `sys.exit` behind would satisfy a substring check. (Mutating both exits to `sys.exit(0)` turns it red; mutating them to `pass` does NOT, and correctly so — the script still exits 1 via the exception that follows, so the behaviour is unchanged. An equivalent mutant, recorded rather than papered over.) The threshold scan also now covers EVERY workflow, not just `ci.yml`, and says plainly what it cannot see: a threshold inside a shell script called from a `run:`, or inside the external reusable workflow behind `pr-quality.yml`, which lives in another repo. One more correction: the promotion_eval scar is **history, not a live hole** — deleting that line no longer leaves the suite green, because a regression test was added when it was fixed; the lesson survives, the defect does not. `overwrite: true` on the upload so re-running a failed job cannot fail on a duplicate artifact name. **A CI/supply-chain lens cleared it to land and verified what I had not:** `uv.lock` IS updated in the commit (pytest-cov 7.1.0, coverage 7.16.0, wheels sha256-pinned for cp312/13/14) and a fresh `uv sync` leaves it byte-identical, so CI cannot silently re-resolve; test SELECTION is identical with and without `--cov` (1748/10/11 across 8 runs), so nothing paid is un-skipped; the artifact name is unique; the XML carries ONE absolute path (`<sources>`) and no source text, and grepping it for secrets matches only module FILENAMES; and cost is **$0** — the repo is PUBLIC so Actions minutes are free. Runtime overhead measured by ALTERNATING A/B pairs: **mean -0.9 s, below run-to-run noise** on a 69-84 s suite (Python 3.14 coverage rides `sys.monitoring`). Worth recording that the reviewer's own first, non-alternating measurement read 44.9 s vs 64.4 s — a cold-vs-warm artifact, and exactly the number a careless benchmark would have published. Three more of my overclaims fixed from that lens: the workflow comment said "coverage can never fail this job" when a report-write error exits 3 (and my own non-vacuity step now fails it deliberately) — narrowed to "the PERCENTAGE can never fail"; the floor `pytest-cov>=6.0.0` was arbitrary and 6.x is a different major, raised to `>=7.0.0` since the floor is load-bearing on the UNLOCKED `pr-quality` path that installs from PyPI; and `_pytest_step()` matched any step whose run mentioned "pytest", so a `pip install pytest` would have reddened a required check — now matches `uv run pytest`. Also recorded in README and TEST_STRATEGY: the number covers the HERMETIC suite only, since `postgres`-marked tests run in their own job without coverage. One reviewer suggestion was declined with a reason: `if-no-files-found` stays `warn` rather than the sibling's `ignore`, because a missing report is already a hard failure at the preceding step, so the warning is a breadcrumb beside it — and `ignore` would erase even that on the Ruff/Pyright-failed path where pytest never ran. **A perf/cost lens closed it as free, with the honest numbers rather than the flattering ones:** on the real runner the pytest step is 77-266 s WITHOUT coverage (n=28, median 91 s) and 89-94 s WITH it (n=2, median 92 s) — a **+0.5% median delta**, the two coverage runs landing at the 36th and 71st percentile of the baseline distribution. Isolating the true tracer cost by min-of-N (robust to added noise) gives **+15.6%, ~13 s**, which then disappears into a runner variance band of ±20 s. Artifact is 204 KB raw / **13 KB zipped**, 14-day retention; stripping XML markup leaves **35 characters of text in the whole file** (one `<source>` path) — no source code, no secrets. The `test` job has **no `env:` block at all**, at job or workflow level, so it structurally cannot make a paid call; providers default to `stub`; and collection is byte-identical with and without `--cov`, so nothing paid is un-skipped. The coverage table is **42 lines of a 2,888-line log** (1.5%), and `skip-covered` cuts it from ~118 rows. **Methodological note worth keeping:** TWO independent reviewers' first local A/B measurements showed coverage running FASTER than baseline — physically impossible, and a signal fully swamped by machine load. Both caught it and re-measured (alternating pairs; CI data). A careless benchmark here would have published a negative overhead. Blocking condition now has an OWNER: **[#321](https://github.com/imrohitagrawal/citevyn/issues/321)** (record a baseline across three `main` runs, then decide) — the artifact has 14-day retention, so it cannot be reconstructed later. |
| [#306](https://github.com/imrohitagrawal/citevyn/issues/306) | CSP blocks every Satoshi font file: `font-src` allows `api.fontshare.com` but the files are served from `cdn.fontshare.com` | frontend / security headers | Low — cosmetic, but silent | Found LIVE in Chrome during the #300 walkthrough (2026-09-03). **FIXED — PR #317 MERGED to `main` (`22ea68f`), branch deleted. NOT deployed.** One host added to `font-src`, verified FIRST-HAND rather than taken from the issue: `curl 'https://api.fontshare.com/v2/css?f[]=satoshi@400,500,700,900'` returns `src: url('//cdn.fontshare.com/wf/...woff2')` for 4 weights x 3 formats = the 12 blocked requests. Measured BOTH ways against the production build served by the backend: **12 CSP violations and 12 blocked requests with the old policy, 0 and 0 with the fix.** **The issue's suggested test would NOT have caught this**, which is the more useful finding: it proposed parsing `index.html` for external origins, but `cdn.fontshare.com` appears NOWHERE in `index.html` — it exists only inside the stylesheet the browser fetches afterwards, so no static read of our own markup can see it. The font-file host cannot be discovered; it has to be DECLARED. `test_csp_covers_the_pages_real_origins.py` therefore parses `index.html` for stylesheet origins and maps each to its provider's font-file origin in an explicit table a human must extend, with a partner assertion that fails if the parser ever stops finding stylesheets (otherwise the whole file passes vacuously the day someone self-hosts). 3 mutations, all killed — including dropping the GOOGLE font host, proving the guard covers both providers and not just the one being fixed. **Correction to the issue's framing, recorded because it changes what this fix is worth:** it says the page "silently falls back to a system font" and "the brand typeface never renders". Nothing falls back — **no `font-family` in the codebase uses Satoshi at all** (both `grep` hits are comments; the tokens use Geist). Measured live: `body` computes to `Geist, system-ui, sans-serif` and `document.fonts.check("500 16px Satoshi")` is false, with AND without the fix. What the block actually produced was 12 console errors per page load, which Chromium emits per `src` URL at `@font-face` parse time even though it never fetches them. So this fix removes 12 errors and makes the policy match what the page declares, but **changes no rendered pixel** — filed as **[#316](https://github.com/imrohitagrawal/citevyn/issues/316)** (the page pays for a render-blocking Fontshare stylesheet and a preconnect for a typeface it never applies; the fix is an owner call between wiring Satoshi up and deleting the link). No `preconnect` added for `cdn.fontshare.com` as the issue suggested: with nothing using Satoshi the files are never fetched, so it would be pure waste. **Security review: SHIP, 0 blockers** — the assembled `_CSP` string was diffed old-vs-new mechanically (not the source diff, since the string is now split across two lines, which is exactly how a second change would hide) and every other directive is byte-identical. What `font-src` admits is narrow: no script, no stylesheet, no fetch, no frames; font fetches are anonymous by spec so there is no credential or exfiltration path. The residual is font-parser memory safety (CVE-2020-15999 was a real in-the-wild 0-day), and SRI is NOT an available mitigation — `integrity` applies to `<link>`/`<script>`, never to `url()` inside a fetched stylesheet. Two review findings fixed here: the partner assertion pinned only NON-EMPTINESS, so an unquoted attribute (`href=https://...`, valid HTML5) or a protocol-relative `href` would drop a provider from coverage and leave the suite GREEN — it now pins the exact origin SET, and both forms plus an undeclared-provider case are mutation-proven to fail; and my own code comment still repeated the "falls back to a system font" wording the commit message refutes. **Advisory left for the owner, deliberately not folded in:** `object-src` inherits `'self'` rather than `'none'` (free hardening, pre-existing) — tightening a security header is as much an owner call as widening one, so it is not being smuggled into a PR opened to widen one. |
| [#296](https://github.com/imrohitagrawal/citevyn/issues/296) | Deploy runbook omits `--build-arg VITE_API_DEMO_KEY` — a by-the-book deploy ships a frontend that cannot call the API | infra / deploy / observability | High — caused ~1 h of production outage | Found deploying PR 14 (2026-09-02, release v6 → fixed in v7). `DEPLOY_FLY.md` §4.1 now carries the argument and a post-deploy bundle check; still open: a mechanical check in `deploy-verify`, and the email-failure log line that prints only the bare event name (`extra` is dropped by the `%(message)s` format) |
| [#293](https://github.com/imrohitagrawal/citevyn/issues/293) | Magic-link recovery cannot replace a forgotten password on an account that still has one | backend / frontend / auth | **DONE (PR 15 = PR #298)** | Owner-approved 2026-09-02 with two guardrails: (1) same-session, one-shot, 10-minute waiver stamped on `auth_sessions.magic_link_verified_at` (migration 0013), never the body, never another session; (2) notification email on every magic-link sign-in and every password set/change. `password_step_up` on `/me`; the nudge and the modal use it. See `docs/ADR/0004-user-accounts.md` |
| [#294](https://github.com/imrohitagrawal/citevyn/issues/294) | ADR-0004 PR 14 advisory follow-ups (magic-link login + password) | backend / frontend / auth | Low — none a bypass | ADR-0004 PR 14 T3 review. Six advisory items: per-address `magic_link` bucket lets anyone spend a victim's 5/hour allowance (accepted trade-off, in the ADR residuals); nudge/toast overlap on narrow viewports; nudge focus after Done; stale `has_password` edge (self-healing via revocation); private `_to_naive_utc`/`_now` copies (precedent in `oauth.py`); `auth_login` 429 copy says "queries" (pre-existing); from PR 15's final round: the magic-link claim's `notice_suppressed` branch and the 503-vs-429 split in `email_notice_allowed` lack bite-proofs, and a limiter outage also sets `notice_suppressed` (docs describe only the ceiling) |
| [#59](https://github.com/imrohitagrawal/citevyn/issues/59) | Embeddings: additional providers behind the seam + scale tuning (Voyage/OpenAI, HNSW recall, corpus refresh) | embeddings | Low (at scale / if Gemini insufficient) | #51 / PR #56, ADR-0003 |
| [#84](https://github.com/imrohitagrawal/citevyn/issues/84) | CiteVyn-meta maturation: ~~name recognition~~ (**item 1 done, PR #172** — single-token speech-to-text aliases (`sitewin`, `citevin`, …) route + canonicalize. The two-word `"site win"` form is a tested MISS: three adversarial rounds showed surrounding-token rules cannot separate it from ordinary English (`may the best site win!`), so it needs intent detection over the whole utterance — see the issue thread before retrying a regex), real-embedder no_answer golden, golden-in-CI, ~~offline-copy convergence~~ (**done** — `frontend/src/lib/citevynAliases.ts` mirrors the guardrail pattern; a pytest drift guard parses the TS list), ~~refusal-copy nudge~~ (**done** — `DEFAULT_UNSUPPORTED_REFUSAL` now names CiteVyn itself), `/about` deploy | backend / frontend | Low (the alias instance hits the owner's own demo flow) | #49 / PR #83 review; #169 live verification |
| [#119](https://github.com/imrohitagrawal/citevyn/issues/119) | Conversation memory: scale to long conversations (rolling summary via `sessions.summary` + LLM standalone-question rewrite + token-budgeted generator context + `(session_id, created_at)` index) | backend / RAG | Low (current design is constant-cost per turn; this adds depth) | live-test review |
| [#125](https://github.com/imrohitagrawal/citevyn/issues/125) | Eval harness: **most landed** (PR #132 chunk-level identity + MRR/precision@1; PR #133 distractor corpus + context precision/recall; PR #134 golden growth 31→50). **Remaining:** human-labeled judge-calibration subset (judge-vs-human agreement) | eval / RAG | Low (remaining piece is calibration, not gating) | Item 2 eval-hardening plan review (deferred) |
| [#174](https://github.com/imrohitagrawal/citevyn/issues/174) | Answer path: an uncited, non-refusal answer was returned with ALL retrieved chunks attached at `confidence=high` — citations strongest where grounding was weakest. **Fixed in PR #176.** Deploy note: flush `answer_cache`, since pre-deploy ungrounded answers replay from cache for the 24h TTL | backend / answer | — (fixed, PR open) | #175 adversarial review |
| [#148](https://github.com/imrohitagrawal/citevyn/issues/148) / [#150](https://github.com/imrohitagrawal/citevyn/issues/150) / [#151](https://github.com/imrohitagrawal/citevyn/issues/151) | Dependabot: fastapi runtime bump + two `actions/*` MAJOR bumps (v4→v7). Left unmerged **by policy** — `DEPENDABOT_TRIAGE.md` requires a named Backend-tech-lead / Ops reviewer for these tiers, unlike dev-only bumps. All CI-green and rebased | deps | Owner-gated | DEPENDABOT_TRIAGE.md |
| — | `DEPENDABOT_TRIAGE.md` describes a `release-blocker` label and a nightly demo-readiness gate that checks for it, but **no automation applies the label** (all four open dependabot PRs carry only `dependencies`), so that gate can never fire | ops / ci | Low (policy-vs-reality drift) | this session |
| [#170](https://github.com/imrohitagrawal/citevyn/issues/170) | ~~Corpus: `claude_code.md` has no installation content~~ **(fixed). The two paths it did NOT reach are fixed too, structurally, in #178: `db/seed/seed_catalog.py` now ingests the shipped corpus instead of carrying a copy, and the frontend offline KB has a Claude Code install branch.** Original: `claude_code.md` had no installation content, so "How do I install Claude Code?" refuses (identical single-turn and as a follow-up — a corpus gap, not retrieval) | corpus / worker | — (fixed) | #169 live verification |
| [#178](https://github.com/imrohitagrawal/citevyn/issues/178) | ~~Corpus content lived in FOUR places (worker sources / conftest fixture / `db/seed` / frontend KB) and drifted~~ **Fixed: `db/seed/seed_catalog.py` now runs the real ingestion pipeline over `app/worker/sources/*.md` (copy deleted — `make demo` needs no separate ingest step because seeding IS the ingest); the two copies that cannot be derived are covered by drift guards (`backend/tests/test_corpus_single_source.py`, `frontend/src/data/knowledgeBase.corpus.test.ts`) that fail the build when a corpus edit invalidates them; the frontend Claude Code install-routing bug is fixed; `npm test` now runs in CI and the frontend workflow triggers on corpus edits** | corpus / db / frontend / ci | — (fixed) | #170 review |
| [#163](https://github.com/imrohitagrawal/citevyn/issues/163) | ~~Worker: `Document.content_checksum` is a misnomer (hashes name+title, not content) + `IngestionRunner` still defaults to the retired `sha256:mvp-snapshot-2` placeholder with a now-backwards docstring~~ **(fixed, PR pending — column renamed to `identity_checksum` via migration **0006** with a reversible downgrade; `source_version_hash` is now a required kwarg).** Deploy note: **0006** is the rename, so the API and worker must be deployed TOGETHER with it. Migration 0005 (`provider_calls`, #184) is additive and safe to apply ahead of the app — the earlier text said 0005 for both, which would have had an operator coordinate the deploy around the wrong migration and apply the breaking one unguarded | backend / worker | Low (latent clarity/correctness; real content fingerprint now lives in `cli.content_version_hash`) | PR #162 adversarial review (F5 / P3) |

## Recently closed

- **[#163](https://github.com/imrohitagrawal/citevyn/issues/163)** — `Document.content_checksum`
  renamed to `identity_checksum` (migration **0006**, reversible); `IngestionRunner` now REQUIRES
  `source_version_hash` instead of defaulting to a retired placeholder. PR #187. NB the branch was
  cut before #184 and both migrations claimed `revision="0005"` — alembic would have seen two heads
  and `upgrade head` would have failed. **Any branch cut before #184 has this hazard.**

- **[#167](https://github.com/imrohitagrawal/citevyn/issues/167)** — a Redis outage no longer
  reports `index_unavailable`. PR #190. The bigger find: `error_response` returns
  `HTTPException(detail=envelope)` and nothing flattened it, so the wire body was
  `{"detail": {...}}` — the frontend read `body.error`, got `undefined`, and the new branch could
  **never fire in production**. Its three frontend tests passed *vacuously*. Now flattened for
  EVERY error code, which also fixes an `API_SPEC` §4 violation.

- **[#168](https://github.com/imrohitagrawal/citevyn/issues/168)** — DEMO_CHECKLIST routes/port
  corrected + a guard test. PR #191. The guard's first version exempted **13 of 16 routes**; the
  reviewer reintroduced #168's original defect verbatim and the suite stayed green. Now scoped to
  the disclaimed token and verb-aware.

- **[#178](https://github.com/imrohitagrawal/citevyn/issues/178)** — corpus is single-source:
  `db/seed/seed_catalog.py` runs the real ingestion pipeline over `app/worker/sources/*.md`
  instead of carrying a copy. PR #192. Took three rounds: the bootstrap originally wrote **stub
  vectors** that survived into a live index (vector arm enabled, ranking by SHA-256 hash distance,
  `/health/index` reporting healthy). Final fix is a `NullEmbedder` seam so they are never written.

- **[#153](https://github.com/imrohitagrawal/citevyn/issues/153) Layers 2, 3, 5** — PRs #188/#189.
  §9 daily budget (soft $5 warn / hard $10 stop, transient not refusal, SQL-summed since midnight
  UTC so restart-proof, fail-closed), concurrency cap, `GET /v1/admin/budget`, and `make budget`
  wired into deploy-verify. **Layer 1 completed for the embedder** in PR #196.

## Open follow-ups filed this session

| Issue | Why it matters |
|---|---|
| [#195](https://github.com/imrohitagrawal/citevyn/issues/195) | **Release blocker 9 is worse than "untested": rollback to `v0.9.0` is IMPOSSIBLE.** Its image cannot boot (uvicorn shebang points at a build-stage path), and `rollback.sh` rebuilds from the target tree. The `make deploy-verify` drill then found the deeper cause: rolling back across a forward-only migration boundary cannot work at all (`Can't locate revision identified by '0006'`). **FIXED, PR #204.** `rollback.sh` refuses up front (override `--allow-migration-mismatch`, plus `--base-ref` so a *second* rollback cannot reason from a detached HEAD); `restore.sh` is atomic (`--single-transaction`); the data-recovery drill is crash-safe (`_drill_lib.sh` + EXIT/INT/TERM traps — every exit path restarts the writers, covered by `tests/shell/test_drill_crash_safety.sh`); and the gate reports which rollback path a run actually proved plus production's real state. **Both drills are now PROVEN end to end**: a live run scored 42 passed / 0 failed against a real prod stack, including the code rollback + roll-forward with the full probe suite re-run at each stage (RELEASE_PLAN §10 blocker 9 carries the output). Remaining, and documented as unproven: the CROSS-generation §4.2 recovery, and a *published* same-generation release pair (the drill used local unpushed tags). |
| [#197](https://github.com/imrohitagrawal/citevyn/issues/197) | **CLOSED — won't build _as specified_.** #153 Layer 4 asked to "persist the per-user rate limiter". (1) **The persistence half already shipped**: `infra/docker/prod.env.example:86` sets `CITEVYN_REDIS_URL`, so `_build_limiter` (`backend/app/core/rate_limit.py:338`) selects `RedisRateLimiter`; the prod bucket is a Redis `ZSET` that already outlives an api restart. There is no in-process limiter in prod to persist. (2) **The "per-user" half does not exist and cannot be delivered by persisting anything**: `require_demo_api_key` returns the *constant* `DEMO_USER_ID` (`backend/app/core/security.py:29,59`) and `_bucket_key` keys on it (`rate_limit.py:256`), so all demo traffic shares ONE bucket, `citevyn:rl:demo_user`. ⚠️ **This means the global-lockout hazard #197 warned about is already LIVE, not averted** — 30 successful queries from one visitor deny the demo to everyone for an hour, and the Redis path means an api restart no longer clears it. Successor filed as [#203](https://github.com/imrohitagrawal/citevyn/issues/203) (per-visitor identity) — that, not persistence, is the remaining work, and it blocks a public URL. Layer 3 (§9 daily budget, PR #188) remains the real spend control. Branch `feat/153e-persist-rate-limiter` retained. |
| [#183](https://github.com/imrohitagrawal/citevyn/issues/183) | ~~`postgres-migrations` never runs on push to `main` (PR-only `if:`, null payload on push).~~ **CLOSED — PR #201.** Also adds a test that parses every workflow and fails on an unintended `pull_request`-only condition, so the class of silent skip cannot recur. |
| [#208](https://github.com/imrohitagrawal/citevyn/issues/208) | ~~Intent router: a flag token hijacks a prose question into `exact_lookup`, so a well-covered answer refuses.~~ **FIXED.** The orchestrator now retries an ungrounded `exact_lookup` through hybrid retrieval **with flag-shaped tokens stripped from the retry query** — the intent switch alone was measured against the real corpus and still refused. `classify_intent` untouched. Verified live: the reported question answers with 2 citations while the off-corpus refusal and the bare flag lookup both still behave | backend / answer | — (fixed) | live UI testing 2026-07-20 |
| [#221](https://github.com/imrohitagrawal/citevyn/issues/221) | ~~**No favicon ships.**~~ **FIXED.** `frontend/public/` now ships `favicon.svg` (theme-aware via `prefers-color-scheme`), `favicon.ico` (16/32/48), `apple-touch-icon.png` (180), `icon-192/512.png` and `site.webmanifest`, plus `theme-color` meta. The mark is a brand-yellow rounded tile carrying a bold bracket-terminal "C" — verified legible at 16x16 on both light and dark chrome. Assets are GENERATED by `scripts/gen_favicon.py` (stdlib-only; no Pillow/ImageMagick in this toolchain) and a drift test re-runs it against the committed bytes, so a hand-edited binary fails the build. **Class closed:** `backend/tests/test_frontend_assets.py` parses `index.html`, extracts every local `href`/`src`, and asserts each resolves — proven RED against the pre-fix tree. Original:  `frontend/index.html` references `/favicon.svg`, but `frontend/public/` DOES NOT EXIST — verified in production, where `/favicon.svg`, `/favicon.ico` and `/apple-touch-icon.png` all 404, so every tab, bookmark and shared link for the public demo shows a blank icon. Fix is to create `frontend/public/` (Vite copies it verbatim into `dist`, which the API already serves since #213 — no build-config change). Ship the full set (svg + ico + apple-touch-icon + manifest/theme-color), and note NO brand mark exists anywhere in the repo: the wordmark is plain text plus a yellow `01` badge, and whatever is drawn must read at 16x16 against both light and dark chrome. **The class matters more than the icon**: a referenced-but-absent asset survived a build, a Docker image, CI and a prod deploy unnoticed — add a test asserting every local asset href in `index.html` resolves in the build output. Same shape as #217 and the API_SPEC §13 path fixed in #212 | frontend / docs | Medium (first thing a visitor sees) | live QA of the Fly deploy 2026-07-21 |
| [#231](https://github.com/imrohitagrawal/citevyn/issues/231) | ~~**CI tests the frontend on Node 20; production ships a bundle built on Node 22 — nothing reconciles them.**~~ **FIXED.** `frontend/.nvmrc` (containing `22`) is now the single source of truth: both workflows read it via `setup-node`'s `node-version-file:`, `frontend/package.json` declares the matching `engines.node`, and `Dockerfile.api:55` already built on 22 — so everything reconciles **on 22**, the known-good shipped version, exactly as the issue sequenced it. Major-only by design: `.nvmrc` `22` and the tag `node:22-bookworm-slim` both float to the newest 22.x, so patch releases flow in without a repo edit while the major stays locked; a full `22.x.y` pin would drift against the floating tag by construction. The `FROM` line is left literal on purpose — an `ARG NODE_VERSION` would stop Dependabot tracking the base image, trading drift detection for update blindness. **Enforcement is a test, not a comment**: `backend/tests/test_node_version_pin.py` scans the whole population (every `setup-node` step in every workflow, every `FROM node:` in every Dockerfile, the `engines` floor) rather than the three known files, with non-vacuity partners so deleting all of them fails loudly instead of passing over an empty list. It runs in `ci.yml`'s `test` job, which has **no `paths:` filter** and is a required status check, so it fires on a Dependabot PR touching nothing but `Dockerfile.api`. It also catches the trap that `setup-node` gives `node-version` **precedence** over `node-version-file` — a stray literal would leave the `.nvmrc` decorative while looking authoritative. **Consequence for PR #227** (node 22→26, still OPEN): its entire diff is that one `FROM` line, so it now goes **RED** with a message telling the author to move `.nvmrc` in the same PR — the bump is not blocked, just no longer silent. **Measured while fixing:** Node 20, 22 and 26 all produce a byte-identical bundle (sha256 `ce1030…18a4`), so the drift had not yet corrupted an artifact — this closes the gap before it bites. **Known blind spot, recorded not hidden:** `pr-quality.yml`'s `quality-gate` calls a reusable workflow SHA-pinned in `imrohitagrawal/.github` that hardcodes `node-version: '20'`; this repo cannot change it, it never builds or tests the SPA, and the guard fails if a *new* unlisted external Node consumer appears. `@types/node: ^20.16.0` is left as advisory debt | frontend / ci | — (fixed) | sequencing PR #227 |
| [#229](https://github.com/imrohitagrawal/citevyn/issues/229) | ~~**`/health/index` and `GET /v1/admin/index_versions` report `evaluation_run_id: null` even when the index HAS passing evaluation evidence.**~~ **FIXED.** Root cause was the WRITE side, not the read side: neither route had a broken join or a wrong status filter — both correctly serialised a column that migration `0001`, the model and the admin schema declared and **nothing had ever assigned**. `app.services.index_versions.link_evaluation_run` is now its single writer, called by `evaluate_index` inside the same transaction as the terminal run. The pointer names the **newest TERMINAL run, passing or failing** (linking only passing runs would make "evaluated and failed" indistinguishable from "never evaluated" — this defect relocated), so non-null means EVALUATED, not PASSED; documented in `API_SPEC` §14, `DATA_MODEL` §14 and `RUNBOOK` §3.7. Two invariants live in the linker rather than caller discipline: the target index is DERIVED from `run.index_version` (crediting index A with index B's passing run would be strictly worse than the null it replaces), and a `running` or unflushed run is refused (`run_id` is assigned at flush, so an unflushed run would write NULL over good evidence). The gate is unchanged and still re-derives evidence via `_latest_completed_run` — a single mutable column must not become the authority for a security-relevant gate. No migration; no `/health/index` payload key added. **Production `v1` stays `null` until `citevyn-worker evaluate` is re-run there** — the fix writes forward, it does not backfill | backend / ops | — (fixed) | live verification of #216 |
| [#226](https://github.com/imrohitagrawal/citevyn/issues/226) | ~~**Retrieval's Tier-3 provenance check reads the ACTIVE index's stamp, not the one being retrieved.**~~ **FIXED — both halves, fail-closed on dual-active.** `_active_index_stamp` now reads the row named by `active_index_version` **by primary key, whatever its status** — the same value the three arms put in their `Document.index_version ==` filter — so doc scoping and the provenance gate can no longer mean different indexes. The status-based lookup survives only as the unscoped fallback. Second half: on a **dual-active** database that fallback used to return `None`, which the shared predicate reads as "unknown provenance ⇒ allow", so the arm ran without knowing whose vectors it scored. The trap was that `None` had to carry two meanings with **opposite** safe answers — "no stamp recorded" (legacy / stub-seeded ⇒ ALLOW, load-bearing for the seeded demo and every hermetic test) and "cannot tell which index" (⇒ DENY). Hardening the `None` arm would have killed the vector arm for every stub-seeded local index, so a distinct `IndexStampStatus.ambiguous` was added instead and only *it* fails closed; a test pins **both directions** so neither state can be folded back into the other. `answer_policy_version` bumped **v5 → v6**: answers cached from either state were built with the arm running, and a cache hit returns before the corrected gate can run, so without the bump the pre-fix answer replays for the 24h TTL (on dual-active `source_version_hash` is a constant `""`, so nothing else in the key invalidates them). No migration — it reads existing `index_versions` columns. Six mutations verified with grep confirmation, including the trap one. Two siblings found during review and deliberately left OPEN: **[#265](https://github.com/imrohitagrawal/citevyn/issues/265)** — with ZERO active rows the arms still filter on document *status* alone and the gate has no row to compare against, so an ingested-but-unpromoted index built by another embedder can still run the arm (pre-existing, unchanged here; a test now PINS today's allow so any change is deliberate); and **[#264](https://github.com/imrohitagrawal/citevyn/issues/264)** — `/health/index` resolves its active row with no ordering and no count check, so on a dual-active DB it reports `healthy`/`embedder_match: true` at the exact moment the read path fails closed (measured). #264 is newly *visible* because #226 made the read path correct while the health route stayed as it was. Original: `HybridRetriever._active_index_stamp` resolves by `status == active`, ignoring `active_index_version`, so any caller retrieving against a NON-active index (exactly what the #216 promotion evaluator does) gets the vector arm ENABLED against a mismatched index — meaningless cosine, the silent failure #57 exists to prevent. Measured, not theorised. PR #225 closed it for the promotion path only | backend / retrieval | — (fixed) | adversarial review of #216 |
| [#215](https://github.com/imrohitagrawal/citevyn/issues/215) | **A well-covered question refuses on ONE phrasing while three near-identical ones answer.** `claude_api.md` has a substantive `## Rate limits` section, yet "What are the rate limit**s** **on** the Claude API?" returns 0 citations / `confidence: none`, while "What is the Claude API rate limit?", "How many requests per minute..." and "What does `CLAUDE_API_RATE_LIMIT` do?" each answer with 1 citation. ~~Routing is correct … so this is retrieval, not routing.~~ **NOT retrieval — DIAGNOSED AND FIXED.** The root cause was the **citation validator**: `validation.py` hard-failed whenever the cited indices had a gap, so a correct, grounded answer citing `[1]` and `[3]` was discarded whole and the user saw the generic refusal — indistinguishable from a retrieval miss, which is why this was filed as one. Three occurrences are in the production `audit_events` trail (`citation_validation_failed: citation indices must be contiguous from 1`). Two premises in the original report were also wrong and must not be rebuilt on: `confidence` is **citation density** (`len(cited)/len(evidence)`), *not* a retrieval score, so "the whole corpus retrieves at the floor" does not follow; and on a production-faithful stack the "failing" phrasing ranks the right chunk **#1 at 0.8246**. The real latent retrieval defect found while measuring is the keyword arm's flat 0.5 → tracked separately as #156 | backend / answer | — (fixed with #236) | live QA of the Fly deploy 2026-07-21; corrected by #215 diagnosis |
| [#216](https://github.com/imrohitagrawal/citevyn/issues/216) | ~~**The #210 promotion gate is inert: nothing writes `EvaluationRun` rows**~~ **FIXED.** `citevyn-worker evaluate --index-version <candidate>` (`app/worker/promotion_eval.py`) measures the CANDIDATE index against the SHIPPED corpus and persists the run the gate reads; exit 0 = promotable without `force`, exit 2 = genuinely below threshold. **Design decision:** deliberately NOT the golden runner — it measures `conftest.seed_catalog`, so a row from it would attest to a corpus the candidate does not contain, converting a visible `no_evaluation_run` block into a silent false pass (worse than the status quo). Suite ships as package data under `app/worker/eval_cases/promotion.jsonl` (15 cases, verified present inside a really-built worker image). Zero-case trap preserved in THREE places (loader, runner, gate). `seed_catalog._activate` is now a DOCUMENTED bootstrap exception. Adversarial review added three preconditions that refuse to MEASURE an untrustworthy candidate (raising = exit 1 'invocation unusable', distinct from exit 2 'genuinely regressed'): a nonexistent `index_version` (there is no DB-level FK, so a typo used to persist an orphan 0.0 run), a candidate whose embedding stamp mismatches the configured query embedder, and — under a REAL embedder only — a candidate with zero embedded chunks (the suite is satisfiable from the keyword arm alone, so an index built during an embedder outage would have scored full marks then served production with a dead vector arm). Verified live against real Postgres + real gemini embeddings: wrote a `passed` run, `pass_rate 1.0` vs threshold 0.95, and the gate's own `measured_pass_rate` resolver reports promotable without `force`. Original: **the gate is inert, nothing writes `EvaluationRun` rows**, so every production promote hits `no_evaluation_run` and needs `?force=true`. #212 bought an audit trail and a speed bump, NOT a live threshold — a deliberate, documented trade that should not be permanent, or `force` becomes muscle memory. Needs a runner that persists a real result against a candidate `index_version`; the open design question is which one (the golden runner has the right shape but measures `conftest.seed_catalog`, not the deployed corpus). Trap to preserve: `scoring.py` scores an EMPTY suite `pass_rate: 1.0`, so a zero-case run must never persist as passing. Also covers `seed_catalog.py:152`, which promotes by direct ORM write and bypasses the gate | backend / ops | Medium | follow-up from #210 / PR #212 |
| [#217](https://github.com/imrohitagrawal/citevyn/issues/217) | ~~**README lists endpoints that do not exist**~~ **FIXED.** §8 now lists all 17 real operations (16 paths); `POST /v1/ask` and `GET /metrics` are gone, as is the phantom `/ready`. NB the issue's third claim was **stale** — `POST /v1/admin/ingest` never appeared in the README (only in the issue text). Two defects the issue did not mention were found and fixed too: the quickstart `curl` was wrong three ways (`X-API-Key` not `Authorization: Bearer`, body field `query` not `message`, and the nonexistent `/v1/ask` — replaced with the real two-step session→message flow), and §11 claimed `scripts/smoke.sh` "boots uvicorn against SQLite and hits `/v1/ask`" when it brings up compose/Postgres and only asserts `/health`. The env table's "Bearer token for `/v1/admin/*`" is corrected to `X-Admin-API-Key`. Guarded by `backend/tests/test_readme_endpoints.py`, which parses the §8 rows and asserts each path+verb against `app.openapi()` — proven RED against main's rows. Original:  (`POST /v1/ask`, `POST /v1/admin/ingest`, `GET /metrics`), verified against the live `app.openapi()`. PR #212 fixed the admin rows and the API_SPEC §13 phantom path but left these as out of scope. Same defect class, and the README is the most-read doc in the repo. Worth a test asserting the table against `app.openapi()` — two documents have now carried invented paths | docs | Low | verified during the Fly deploy 2026-07-21 |
| [#210](https://github.com/imrohitagrawal/citevyn/issues/210) | ~~**Index promotion has NO quality gate.** `CITEVYN_INDEX_PROMOTION_MIN_PASS_RATE` is declared in `config.py` and read by nothing; `promote_version` promotes unconditionally.~~ **FIXED.** The gate lives in `promote_version` (the service, so every caller is gated, not just the route): it resolves the candidate's newest **completed** `EvaluationRun` (a `running` run is not evidence), reads `metrics["pass_rate"]` or derives it from `cases_passed/cases_total`, and raises `IndexPromotionBlocked` → **409 `promotion_blocked`** unless `rate >= threshold` (equality promotes). No usable run refuses too — "unevaluated" is not "passing". Because **nothing in the deployed app writes `EvaluationRun` rows**, every real promote — first deploy, corpus correction, emergency index rollback — is refused without the audited `?force=true` override, which records `force`, `measured_pass_rate`, `threshold` and `evaluation_run_id` in the `promote_index` audit row (on the clean path too). The idempotent already-active early-return stays **above** the gate so a harmless retry is not a 409 — but that no-op is *not* the dual-active repair: converging a drifted database means promoting a **different** version, which runs the demotion loop below the gate and so needs `?force=true` too. Docs re-pointed: `DEPLOY_FLY` §4.3 / §6, `RUNBOOK` §3.7 + §5.3a, `RELEASE_PLAN` §7 (gate 1 enforced, gates 2-5 annotated operator-verified), `API_SPEC` §13 + §15, `rollback.sh`, `DEMO_CHECKLIST`, and the `config.py` comment that said "NOTHING READS THIS SETTING" | backend / ops | — (fixed) | T3 review of the Fly deploy branch |
| [#237](https://github.com/imrohitagrawal/citevyn/issues/237) | ~~**`_CITATION_RE` counts `[n]` inside code spans / fenced blocks as citations.**~~ **FIXED (PR #261), corrected by PR #262.** #261's scanner had a second, opposite defect found after it merged: a closing fence that was not BARE (`` ``` (that is all) ``) left the fence open, so every citation after it was silently DELETED — the answer still cited `[2]`/`[3]` with no such cards, no refusal, no audit reason. #262 closes a sloppy fence only as a LAST RESORT (when no bare closer is reachable later), which fixes that without breaking fence NESTING or treating an info-string opener inside a fence as a closer — the latter discarded whole answers as out-of-range. Measured with a LABELLED fuzz (each marker placed as PROSE or CODE by construction, never classified after the fact): 0 losses / 0 phantoms / 0 new refusals across 39,002 well-formed CommonMark answers. A cardinality-only oracle ("serves fewer than main") cannot see this class of defect and reported a net improvement while thousands of citations were being deleted — use labelled ground truth for any future change here. `answer_policy_version` v4 → v5 with it. `validate_citations` now scans a code-stripped copy of the answer, via `_cited_markers = findall(_strip_code(t)) or findall(t)` — the mandated `or`, so the strip can never empty the set and no new refusal or paid `exact_lookup` retry can exist (measured: without the fallback, `no_answer=True, GENERATE_CALLS=2`; with it, `no_answer=False, GENERATE_CALLS=1`). Two invariants hold by construction and are pinned as a property test: the cited set is a SUBSET of the raw scan, and it is empty iff the raw scan is empty. Fixing this also removed a live FALSE REFUSAL — `arr[9]` in a code span with 3 evidence bullets tripped the out-of-range branch and discarded a correct answer — . It does NOT close the #174 grounding-gate hole (an answer citing nothing in prose but carrying one bracketed number inside a fence is still served as grounded) — the `or` fallback restores exactly that set, so the outcome is identical before and after; that is the documented price of the fallback, not an oversight. **Covered:** ``` fences, `~~~` fences (incl. unterminated), inline spans. **Deliberately NOT covered:** 4-space/tab indented blocks and bracketed indices in plain prose — a line scanner cannot separate indented code from indented prose (blockquote, table row, heading, nested bullet) without a block parser, and every misjudgement DELETES a real citation, which is silent and strictly worse than the phantom being fixed; the shipped corpus has no indented code and the prompt never asks for code. The scanner is line-oriented because a first draft using multi-line regexes + a 4-space rule lost real citations 10 ways (all 10 are now regression tests) | backend / answer | ~~Medium~~ **RESOLVED** | #215 plan attack; rationale corrected during #215 review; fixed #237 |
| [#236](https://github.com/imrohitagrawal/citevyn/issues/236) | ~~**Citations carry no index, so the frontend renumbers by array position.**~~ **FIXED with #215** — splitting them would have shipped a wrong-citation bug. While the validator rejected gaps, marker and position were always equal, so this was latent; the moment gapped citations are served, `[1]`+`[3]` renders as cards **1** and **2** while the prose says `[3]`. The wire now carries `marker` (the 1-based evidence index the model actually wrote) and the frontend renders it. `answer_policy_version` bumped **v2→v3** to evict pre-marker cache rows. NB `stub.py` always emits exactly `[1]`, so `marker == index + 1` **by construction** in all 1357 hermetic tests and in the whole eval harness — locked eval numbers are worthless as evidence here, and only a fake generator emitting a gap catches a regression | backend / frontend / API | — (fixed) | #215 diagnosis |
| [#234](https://github.com/imrohitagrawal/citevyn/issues/234) | **Chat: multi-line answers render as one run-on blob.** `white-space: pre-wrap` was added in `7e9bc92`/`ad062bb` and deleted by `2503dd4` (the landing design port overwrote the chat CSS); `git show 2503dd4 -- frontend/src` shows two `-` deletions and zero additions. 94 UI tests missed it for two compounding reasons: jsdom does not compute CSS, **and** every canned answer (the live stub's plus all 15 offline `KB` entries) is a single unbroken line, so even a browser test had nothing to look at. There was also no class to attach the rule to — `ChatView.tsx` rendered `className=""` when idle. Fixed by a stable `message-body` class + a Playwright guard asserting **rendered line-box count** rather than the CSS property, so it survives the markdown container overriding `pre-wrap` later | frontend | Medium (every multi-line answer on the public demo) | #215 diagnosis |
| [#203](https://github.com/imrohitagrawal/citevyn/issues/203) | ~~**The demo rate-limit bucket is GLOBAL and now restart-proof.**~~ **FIXED** — the limiter is now keyed per visitor (salted HMAC of the client IP, IPv6 collapsed to /64, fails closed to one shared bucket) with a global backstop; the audit identity is unchanged. Surfaced while closing #197: `DEMO_USER_ID` is a constant and the Redis limiter is already live, so 30 successful queries from ONE visitor deny the demo to everyone for a rolling hour, and an api restart no longer clears it. Needs per-visitor identity (session and/or IP). **Blocks publishing a public URL** (the demo half of #153); does not block cutting the tag. |

- **[#161](https://github.com/imrohitagrawal/citevyn/issues/161)** — CLOSED, no demonstrable
  behavioural impact. The `${v:1:-1}` observation was linguistically correct (bash 4.2+; bash 3.2
  raises "substring expression < 0"), but no failing invocation was ever produced. The code was
  hardened anyway — #179 repaired four silently-failing `test_env_guard.sh` cases and #181 gave
  `tests/shell/` a runner matrixed over ubuntu-latest **and macos-latest** (bash 3.2). With that
  lane green on both, the guard is proven on 3.2. Reopen with a concrete reproducer.

- **[PR #39](https://github.com/imrohitagrawal/citevyn/pull/39)** — CLOSED as superseded. The
  landing page shipped by another route: `frontend/src/components/LandingPage.tsx` (+
  `landing-sections.tsx`, `Hero.tsx`, `styles/landing.css`), wired via `App.tsx` and covered by
  `LandingPage.test.tsx`. Salvage was checked first: `frontend/playwright.config.ts` is already on
  `main`; `.agents/skills/**` is superseded by `SKILL.md` + `skills-lock.json`; `.vite/deps/*` were
  accidental build artifacts. Branch retained. (Aside, still true: `make e2e` runs a curl smoke and
  its help text still points at "Slice 11" for the Playwright upgrade.)

- **[#153](https://github.com/imrohitagrawal/citevyn/issues/153) Layers 0 + 6** — PR #182
  (main `112c3ff`). Judged eval now runs on `main` / `full-eval`-labelled PRs only, at FULL
  coverage. Case sampling was measured and REJECTED (43/60 cases carry a zero-tolerance oracle that
  sampling switches off, not averages down; ceiling was ~28%). Also fixed: the job's PR-only `if:`
  meant it never ran on `main` at all. New `docs/COST_CONTROLS.md`.

- **[#153](https://github.com/imrohitagrawal/citevyn/issues/153) Layer 1** — PR #184
  (main `3464aa3`). Per-call cost metering: `provider_calls` + migration 0005, priced by
  provider+model. **Layers 2-5 remain open** — see `docs/AUTORUN_HANDOFF_2026-07-20.md`.

- **[#82](https://github.com/imrohitagrawal/citevyn/issues/82)** — No CI job built/booted the
  api+worker images, so a container-runtime break (interpreter/CMD, which `docker build` does
  NOT catch) shipped green (the class that let the 3.14 bump merge non-booting). Fixed via
  `fix/82-ci-image-boot-smoke`: `infra/docker/scripts/image_smoke.sh` builds+BOOTS the images
  (api boots under `CITEVYN_ENVIRONMENT=local` → GET /health=200; worker execs
  `python -m app.worker.cli list-sources` exit 0), wired as `make image-smoke`, a CI PR-gate
  job (`image-smoke`), and a release.yml load→boot→push gate so a non-booting image fails the
  release BEFORE `:latest` publishes. dependabot groups the uv-builder + slim-runtime `FROM`
  refs so a minor bump can't drift the interpreters apart. Verified locally: smoke passes on
  the real images AND fails on a deliberately non-serving image (proven gate, not a rubber stamp).

- **[#87](https://github.com/imrohitagrawal/citevyn/issues/87)** — Retrieval returned
  no_answer for legitimate source-named questions ("How do I install the Codex CLI?"). Root
  cause was NOT domain misrouting (routing is correct: domain=codex): on the LIVE Postgres
  path (real embeddings) the repro already returns evidence, but it survived HERMETICALLY
  (SQLite, vector arm off) because the thin conftest codex/gemini fixtures lacked content the
  real shipped corpus has, so scoped keyword retrieval found nothing. Fixed via
  `fix/87-source-named-retrieval-regression`: enrich conftest.seed_catalog codex (install +
  OPENAI_API_KEY) and gemini (streaming) chunks to mirror the real worker sources; mirror
  install into db/seed; add regression guards — a hermetic retriever test (asserts the RIGHT
  content is retrieved), golden case codex_011, and CI-gated `--postgres` eval case
  codex_lit_install. golden 49/51→52/52; judged eval improved (overall 10/15→11/16, judge
  4.16→4.29, groundedness 0.818→0.833, paraphrase held 0.0, refusal leaks 0). No orchestrator
  code change.

- **[#93](https://github.com/imrohitagrawal/citevyn/issues/93)** — Seed modules logged the
  full `CITEVYN_DATABASE_URL` (password included) to stdout; `deploy.sh`/CI ran them, so the
  credential landed in deploy/CI logs. Fixed via `fix/93-redact-seed-db-password`: a shared
  `db.seed.redact_database_url` helper (SQLAlchemy `hide_password=True`; bails to a placeholder
  on an unparseable URL or a raw-`@`-in-password that `make_url` would mis-split) routes both
  success prints. Package-relative import so it resolves under BOTH the deploy image layout
  (`python -m seed.*`, `PYTHONPATH=/db`) and repo-root/CI (`python -m db.seed.*`). TDD + a
  deploy-layout import regression test (the review caught the absolute import breaking prod).
  Verified: live seed run prints `citevyn:***@…`, 20/20 unit tests green, lint+typecheck clean.

- **[#112](https://github.com/imrohitagrawal/citevyn/issues/112)** — Conversation memory:
  entity-aware CONTENT-NOUN follow-up rewrite. A follow-up naming no product + no bare anaphora
  ("is there a credentials file option?") used to refuse; `condense_question_llm` now resolves
  it via an LLM standalone-question rewrite, wired as a PURE recall-improver on the answer-when-
  grounded path (can't hijack routing). Deterministic regex kept for the hermetic followup gate;
  a new `judge_only` eval flag validates the case on the judged run only. Answered + gate green
  (stable ×3), locked numbers unchanged. See RAG_QUALITY_PLAN §8a-10.

- **[#85](https://github.com/imrohitagrawal/citevyn/issues/85)** — CI flake: `compose-db-smoke`
  `db-verify` raced the pgvector:pg18 first-boot restart (`FATAL: shutting down` / `database
  "citevyn" does not exist`). Both `docker exec psql` calls (`SELECT 1` + `CREATE EXTENSION
  vector`) now retry in a bounded loop (10×2s) that rides out the transient window; the cap still
  hard-fails a genuinely broken boot (no false green). Fixed via `fix/85-db-verify-retry`,
  Makefile-only, verified with fresh-volume `make ci-smoke` ×3.

- **[#120](https://github.com/imrohitagrawal/citevyn/issues/120)** / **[#121](https://github.com/imrohitagrawal/citevyn/issues/121)** / **[#122](https://github.com/imrohitagrawal/citevyn/issues/122)** — chat UX fixes
  (surfaced by live testing): transport errors (429/5xx/network) no longer wear the "NO SOURCE —
  REFUSED" content badge (distinct rate-limit/connection notice, #120); re-asking a failed question
  re-shows the user bubble (#121); autoscroll no longer yanks the view down when scrolling up during
  streaming (#122). Fixed via `feat/chat-ux-fixes`, frontend-only, live-verified.

- **[#92](https://github.com/imrohitagrawal/citevyn/issues/92)** — Worker prod ingestion: source
  docs now ship as package data under `backend/app/worker/sources/` (were unshipped test
  fixtures). MERGED via PR #105 (main `f199a2f`). Verified inside a built worker image + the
  worker ingested a real 33-chunk corpus on Postgres. `HttpFetcher` stays an unwired seam
  (curated license-clean local docs are the intended source, per ADR-0003). Completes Phase 1.
- **[#97](https://github.com/imrohitagrawal/citevyn/issues/97)** — Populate chunk embeddings +
  index provenance (revive the dead vector arm), Phase 1 PR1.1. MERGED via PR #103 (main
  `d3795f6`). OpenRouter/OpenAI `text-embedding-3-small` embedder behind the seam + embedding-aware
  seeders + db/seed backfill + opt-in Postgres eval mode. Proven on real pgvector: paraphrase
  0/5→3/5, overall 13/15 (0.867), zero residue; semantic-discrimination proof real 5/5 vs stub.
  See RAG_QUALITY_PLAN §8a-1. Phase-1 remainder: **#92** (real prod ingestion) still open.

- **[#96](https://github.com/imrohitagrawal/citevyn/issues/96)** — RAG eval harness (Phase 0)
  landed via PR #98 (main `43972a3`). Golden set + retrieval hit-rate + LLM-judge, CI-gated;
  baseline in `docs/RAG_QUALITY_PLAN.md` §8a.
- **[#99](https://github.com/imrohitagrawal/citevyn/issues/99)** — retired `gemini-2.5-flash`
  replaced via PR #100 (main `cc72b01`): primary `gemini-flash-latest` (free) + `openai/gpt-4o-mini`
  fallback (paid, different provider family). Live-verified. Follow-up: refill the judge baseline
  (§8a) via `make eval` during Phase 1.

## Roadmap: real user accounts (ADR-0004)

| Issue | Title | Area | Notes |
|---|---|---|---|
| [#270](https://github.com/imrohitagrawal/citevyn/issues/270) | Real user accounts (login): ADR-0004 implementation, PRs 0-15 | backend / frontend / auth | Owner-approved (`~/.claude/plans/i-also-want-you-hashed-marble.md`), overriding the doless recommendation. **PR 0-4 DONE. The "no further than PR 4 without check-in" allowance was superseded by the owner's own `docs/NEXT_SESSION_PROMPT.md` (dated 2026-09-01, explicitly invoked to start this session) — that document is the explicit go-ahead this note was waiting for, and it extends autonomous authority through PR 11, pausing only before PR 12 (GitHub OAuth).** PR 1: `_get_session_or_404`/`_require_session` now filter on `user_id` + unexpired; mismatch is 404 never 403; mutation-tested. PR 2: security-headers middleware on every response incl. the StaticFiles mount, `/docs`+`/redoc`+`/openapi.json` disabled in production — health-route-behind-admin deferred to **[#273](https://github.com/imrohitagrawal/citevyn/issues/273)**. PR 3: migration `0007` `auth_sessions` + `app.core.auth_sessions.resolve_principal` — every session/message route now resolves a real per-visitor principal (anonymous `anon_<uuid4hex>`, cookie `<id>.<secret>`, only `sha256(secret)` stored) instead of the constant `demo_user`, so PR 1's ownership predicate now actually differentiates visitors; `CORS allow_credentials=False` pinned by test. PR 4: `app.core.passwords` (Argon2id, OWASP params, `asyncio.Semaphore(2)` concurrency bound, dummy-hash timing-parity verify) — no routes yet, new dep `argon2-cffi`. **PR 5 DONE:** migration `0008` adds `users.email`/`users.password_hash` (both nullable, `email` unique) and moves `sessions.user_id`'s FK `RESTRICT`->`CASCADE`; round-trip test proves the CASCADE behaviorally (seeds a session, deletes the owning user, asserts the session is gone) and mutation-confirmed RED when reverted to RESTRICT. **This is the one-way door named in the ADR — must be verified in production before PR 6 ships.** **PR 6 DONE:** `/v1/auth/{register,login,logout,me}` (`backend/app/api/routes/auth.py`) + `claim_and_login`/`revoke_current_session`/`try_resolve_principal` (`app.core.auth_sessions`) + a second, email-keyed rate-limit bucket (`auth_login`, 10/hr default) guarding credential stuffing independent of the existing IP-keyed demo limiter + `AuditAction.login` now actually emitted (`event` in `{register,login,logout}` metadata). The real two-account IDOR test the plan called for is in `backend/tests/test_auth_routes.py::test_two_real_accounts_cannot_read_each_others_sessions` — two genuinely registered principals, not the seeded stand-in `test_session_ownership.py` used before this PR. Claim-on-login and the email-keyed rate limiter are both mutation-confirmed to bite. `/v1/auth/me` deliberately does NOT mint on a missing/invalid cookie (401 instead) — the one place in the app that differs from the mint-transparently pattern PR 3 set for session/message routes, because a stale post-login cookie must visibly 401, not silently re-authenticate as the pre-login identity. 1489 passed / 17 skipped (root, `provider=stub`), was 1472 before this PR. Adversarial T3 review (Workflow tool) found a real CRITICAL_BLOCKER before merge — `claim_and_login` reassigned a browser's ALREADY-authenticated account's sessions to a second account logged into on the same browser (no logout in between); fixed with an `anon_`-prefix allowlist check, mutation-confirmed, regression test added. Also fixed an ADVISORY_DEBT (concurrent duplicate registration surfacing a 500 instead of a clean 422) and two CodeQL findings (a ReDoS-prone email regex replaced with a linear string check; one clear-text-storage alert dismissed as a documented false positive — the deliberate bearer-cookie design). **PR 7 DONE:** honest pricing/FAQ copy — "Saved history" moved from Pro's feature list to Demo's (unlocked by free optional sign-in, not a paid promise), Pro marked "Planned — not billed yet" with CTA "Get Pro"->"Notify me", new FAQ "Do I need an account?". `backend/app/worker/sources/citevyn.md` gets a new "Accounts and saved history" section; `frontend/src/data/knowledgeBase.ts`'s canned Pro answer and the `conftest.py` fixture chunk updated in lockstep; corpus-mirror digest re-pinned (`uv run python -m tests.corpus_mirror --write`). Bundle: 190.22 kB / 60.54 kB gzip (baseline 189.92 / 60.43, +0.11 kB gzip — well under the 62.5 kB budget). Frontend: 91/91 unit tests pass; manually verified in-browser (Pricing + FAQ render correctly) since Playwright's hardcoded `localhost:3000` collided with an unrelated already-running process this session. Footnote "NO ACCOUNT · NO SETUP" is unchanged — it stays true by design (ADR-0004 non-negotiable #1). **PR 8 DONE:** `lib/authStore.ts` (module-level external store, `useSyncExternalStore` binding via `hooks/useAuth.ts` — no auth library, per the ADR) + `credentials:"include"` on every `apiFetch` call + a real 401 interceptor (`api.ts`'s `onUnauthorized`, fires for ANY endpoint's 401, not just auth's own) + lazy-loaded `AuthModal` (`React.lazy`, lands in its own `AuthModal-*.js` chunk) + always-mounted `AccountMenu` in the Header. A **live browser check** (not just the 20 new jsdom/Testing-Library tests) found and fixed a real bug the tests could not see: `.header`'s `backdrop-filter: blur(12px)` creates a CSS containing block for `position: fixed` descendants, so the modal rendered inside Header/AccountMenu resolved its full-viewport backdrop against the 64px header instead of the viewport — fixed with `createPortal(..., document.body)`, the standard pattern for exactly this class of bug. Also found+fixed: a dead `offsetParent`-based visibility filter in the focus-trap that silently disabled the trap under jsdom (no layout engine) — removed, since nothing in this modal is ever conditionally hidden; and `bootstrapAuth()` only normalizing a clean 401 to "anonymous", leaving the store stuck on "loading" forever (unhandled rejection) if the backend is merely unreachable — now fails safe to anonymous on ANY error. Focus-trap/Escape/focus-restoration verified BOTH ways: 9 Testing-Library tests (real DOM events, real focus assertions) AND a live Chrome walkthrough (Tab wraps end-to-end through 5 focusable elements back to Email, Escape closes, focus visibly returns to the "Sign in" trigger). Full register→identity-in-header→sign-out cycle verified end-to-end against a real running backend (sqlite + `provider=stub`), not just mocked fetch. Bundle: main 194.24 kB / 62.03 kB gzip (thin headroom under the 62.5 kB budget — flag to owner before PR 9/10 add more), lazy `AuthModal-*.js` chunk 4.00 kB / 1.74 kB gzip (well under the 9 kB budget). Frontend: 111/111 unit tests. **PR 9 DONE:** The backend claim (PR 6) already reassigns an anonymous session's ownership transparently via the shared cookie — the CURRENT tab needed no refetch (its `session_id` never changes, only who owns it). The one thing that genuinely needed client-side wiring: `AuthModal` gained an `onAuthenticated` callback (fired only on a real successful sign-in/register, not on close-without-submitting), threaded through `AccountMenu` -> `Header` -> `LandingPage`, which fires a confirming toast — "Your conversation is saved to your account." when the current tab already had messages, a plain "Welcome to CiteVyn." otherwise (the auth module has no visibility into chat state on its own, by design). Verified against a REAL backend end-to-end, not just mocked: sent a live message as an anonymous visitor, registered mid-conversation, confirmed the transcript stayed on screen with no reload and the toast fired — then queried the sqlite DB directly and confirmed the `sessions` row's `user_id` had actually moved from the `anon_` principal to the new `usr_` account server-side, not just a client-side illusion. 2 new tests (`LandingPage.authToast.test.tsx`) cover both toast copies; mutation-confirmed. Frontend: 117/117 unit tests (was 115). Bundle: main 195.04 kB / 62.31 kB gzip — still under the 62.5 kB budget but headroom is now ~0.19 kB; **PR 10's history drawer MUST be lazy-loaded (the ADR already calls for this) or the budget breaks**, flagging explicitly for that PR. **PR 10 DONE:** citation hydration fixed first, as the plan required: migration `0009` adds `messages.citations` (JSON, nullable) — the exact wire-shaped citations (marker included) a message had, persisted verbatim at write time in `Orchestrator._persist_messages` for EVERY response path (including a cache hit's `cached.citations`), never reconstructed from `retrieved_evidence` on read (a cache hit persists ZERO evidence rows for its message — proven directly: `test_gapped_citation_markers_survive_the_cache_round_trip` now also asserts the DB row). `GET /v1/sessions/{id}` now returns `citations` on every message; new `GET /v1/me/sessions` (`backend/app/api/routes/me.py`) lists the caller's own sessions (works for anonymous too, per its own docstring — signing in makes history durable, it isn't a requirement to see it in-tab). Frontend: lazy-loaded `HistoryDrawer` (own `HistoryDrawer-*.js` chunk, same `createPortal(document.body)` pattern PR 8's `AuthModal` established) wired into `AccountMenu`'s signed-in dropdown; `resumeSession()` in `useLandingState.ts` fetches+replaces the transcript wholesale and pins `sessionIdRef` so the next question continues the resumed session rather than minting a new one. **A live browser walkthrough — not the 20 new backend + 11 new frontend tests — found a real, previously-invisible backend bug**: `_persist_messages` stamped the user question and its assistant answer with the IDENTICAL microsecond `created_at`, so `GET /v1/sessions/{id}`'s `ORDER BY created_at ASC, message_id ASC` fell back to a random UUID tiebreak that could (and did) render the answer above the question. Nothing had ever exercised this read path end-to-end before the history drawer existed — the live streaming chat renders its own messages client-side and never re-fetches this endpoint. Fixed by giving the assistant message a strictly-later timestamp (`max(_utcnow(), user_created_at + 1μs)`); the regression test asserts the persisted timestamps directly rather than trusting API response order, because a UUID-tiebreak bug is a coin flip that a response-order-only test can pass by luck (confirmed: the naive version of this test passed about half the time with the bug still present). Full register→ask→resume cycle verified end-to-end against a real backend with a real seeded corpus, down to querying the sqlite `users`/`sessions` rows directly. Backend: 1503 passed / 17 skipped (was 1496 before PR 8). Frontend: 128/128 unit tests (was 117). Bundle: main 196.20 kB / 62.76 kB gzip — **now over the 62.5 kB soft target** (still under the 63.5 kB hard-fail line, ~0.74 kB headroom left); `HistoryDrawer-*.js` 2.76 kB / 1.30 kB gzip (well under the 9 kB budget). Flagging the main-bundle trend explicitly for the owner: PR 11 (per-user rate tiers) is small and shouldn't add much, but the budget itself may need revisiting before any further frontend growth. **PR 11 DONE (PR #284, main `2ea10ae`):** a registered caller now gets a higher demo rate limit (default 100/hr, `rate_limit_demo_user_registered_per_hour`) keyed on their `user_id`, instead of sharing the anonymous 30/hr IP-HMAC bucket — `rate_limited_demo()` peeks the cookie via the already-reviewed `try_resolve_principal` (PR 6) and routes a genuinely-registered `usr_`-prefixed id to a new `demo_user_registered` bucket/role, threaded through both the in-process and Redis limiters. The prefix check is a **positive allowlist** (`registered_id.startswith("usr_")`), not a denylist on `anon_` — a T2 review (general-purpose agent) caught the original denylist form as fail-OPEN for an unrecognized future principal shape and it was inverted before merge, mirroring the same fail-closed reasoning already in `auth_sessions.py`'s claim-on-login check. The 429 message appends "Sign in for a higher limit." only for the anonymous tier; the frontend toast (`useLandingState.ts`) mirrors this via `authStore`'s `getAuthSnapshot()`, no new subscription. Two ADVISORY findings from review were documented, not fixed, as out of scope for this PR: (1) a single IP can register several free, unverified accounts to draw a larger share of the shared global rate-limit backstop than before — bounded (still capped at the global ceiling), but a real fairness degradation; registration has no email verification today, which is the actual root cause and belongs to a future email-verification PR, not this one. (2) during the brief auth-bootstrap window a fast rate-limited request could show the anonymous upsell toast to an already-signed-in caller — cosmetic only. Every new test proven to bite (reverted each fix, confirmed RED; restored, confirmed GREEN). Backend: 1507 passed / 17 skipped (was 1503 before this PR). Frontend: 131/131 unit tests (was 128, +2 for the conditional upsell copy); `tsc --noEmit` clean; build succeeds. Bundle: main 196.38 kB / 62.80 kB gzip — still over the 62.5 kB soft target flagged at PR 10, still under the 63.5 kB hard-fail (~0.70 kB headroom); no lazy chunks changed. **PRs 5-11 of ADR-0004 are now all DONE and merged to main. Per `docs/NEXT_SESSION_PROMPT.md`'s explicit instruction, autonomous work PAUSES here — PR 12 (GitHub OAuth) needs an owner design sanity-check before implementation, since it adds a third-party credential flow (state/PKCE, redirect-URI validation, a new `user_identities` table).** Nothing in PRs 5-11 has been deployed (`fly deploy` was out of scope and classifier-blocked this session) — all of it is merged to `main` and CI-green, not running in production. **PR 12 DONE (PR #287, main `210fc64`):** GitHub + Google OAuth login (authorization-code + PKCE S256, DB-backed session-bound `oauth_nonces`, `user_identities` table, migration `0010`/`0011`); identity resolution by `(provider, provider_account_id)` only, never by email; 3 adversarial rounds found and fixed 2 real bugs; live-verified against real GitHub/Google apps with direct DB checks; the pre-existing `_mint_principal` FK-ordering bug on Postgres was fixed in the same branch and the SQLite-FK testing gap tracked as #286. **PR 13 DONE (account linking):** `GET /v1/auth/oauth/{provider}/connect/start` attaches a GitHub/Google identity to the *already signed-in* `usr_` account — the working form of the ADR's promised password-recovery path. Session-freshness gate (`oauth_connect_max_session_age_seconds`, default 20 min; owner-required — bounds a stolen cookie's exploitable window from 180 days to the minutes after a genuine login; residual recorded in the ADR); the callback dispatches on the nonce's `return_intent` (plain string column → **no migration**); `_link_identity` → `LinkResult` never reassigns an identity linked elsewhere, including under a concurrent-insert race (the loser compares the winner against its OWN target), never creates a `User`, never touches `users.email`, never rotates the cookie (byte-identity asserted); `/me`+register+login gain `providers`. Frontend: lazy `ConnectedAccountsDrawer` from a third non-navigating `AccountMenu` menuitem; the OAuth return-trip cleanup now strips only the app's own query keys (pre-existing `replaceState` bug fixed, `utm_*` survives — verified live). **A live browser walkthrough found one real bug the 45 mocked tests could not see:** cancelling the provider's consent screen mid-connect reported "Sign-in failed" to a still-signed-in user, because the denial branch ran before the nonce's intent was known — fixed by consuming the caller's OWN nonce on denial via the same session-bound conditional claim and routing by its intent (`reason=denied`; griefing guard preserved and regression-tested). 20 new backend tests (13 guards mutation-confirmed RED→GREEN), 15 new frontend tests (4 mutations bite). Live-verified on local Postgres + the real GitHub app: connect/start mints a `return_intent=connect` nonce bound to the caller's own session, the freshness gate rejects a 25-minute-old session with no nonce written, the denied path lands on the connect toast with the audit row attributed to the acting account. **The success and already-linked-elsewhere round trips need the OWNER to click the provider's Authorize button** (an OAuth grant — not something an agent may click); the hermetic tests cover both, and the runbook for the manual check is in the PR body. `anonymous: true` for an OAuth account with no verified email → filed as #288 (pre-existing, PR 12); OAuth env-var docs gap → #289; HistoryDrawer focus gap → #290. Shipped as PR #291. **PR 14 DONE (magic-link login + Resend + set/change password):** migration `0012` `magic_link_tokens` (FK CASCADE proven on SQLite *and* real Postgres, downgrade proven); `app.core.token_secrets` (stdlib-only) + `app.core.email_client` (`Protocol` + `ResendEmailClient` via `httpx.MockTransport` in tests + a dev-only `FileOutboxEmailClient`, refused in production by a `Settings` validator); `POST /v1/auth/magic-link/request` always-202 with **statement-count parity** between the match and no-match branches (white-box test counts SQL verbs on both), a dedicated `magic_link` rate bucket (never `auth_login`), one live token per user; `GET …/confirm` is a read-only plain-HTML interstitial (no script/meta-refresh/onload — scanner-safe, regression-tested) and only `POST …/confirm` claims, atomically on `token_id AND secret_hash` (a wrong secret consumes nothing), with a same-origin check (`Origin`/`Sec-Fetch-Site`) as a login-CSRF guard found during implementation and refused BEFORE anything is consumed; `POST /v1/auth/me/password` decides the `current_password` requirement ONLY from the stored `password_hash` (the round-2 CRITICAL regression test passes and bites), 422 not 401 on a wrong current password (a 401 would trip the frontend's sign-out interceptor), and revokes every other session on both first-time set and change; `has_password` on `/me`+register+login. Frontend: `AuthModal` gained `magic-link` and `set-password` modes (in-modal `role="status"` notice, `maxLength=128`), a lazy `Nudge` card after a passwordless `?auth=ok` return with a per-device `localStorage` dismiss, and the (renamed) "Sign-in methods" drawer gained a Set/Change password row; the magic-link/password wire calls live in `lib/authActions.ts` so they ride the lazy chunk. **Bundle: main 198.35 kB / 63,496 B gzip (63.50 kB displayed) — 4 bytes under the 63.5 kB hard line, after three measured trims (wire calls moved out of the eager modules, one shared bump-then-apply helper in `authStore`, and the nudge's chunk file name shortened to `Nudge`); baseline was 63.40 kB.** Backend 1603 passed / 19 skipped (was 1552), Postgres-marked 10/10 against local docker; frontend 179/179 (was 158). PR 14 shipped as PR #295. **PR 15 DONE (#293, PR #298):** same-session password step-up (`auth_sessions.magic_link_verified_at`, migration 0013, one shot, 10-minute window) + sign-in / password-change notification emails; review added a per-address notice ceiling (mail-cannon finding) and a per-user cap on current-password changes (account-camping finding). Bundle note: the 63.5 kB main-chunk figures throughout this row are measured with `VITE_API_LIVE=true` (frontend/.env.local locally, the Dockerfile in production); a build without it is ~0.85 kB larger and is not what ships. **Bundle ceiling raised twice, deliberately: 63.5 -> 64.0 kB in the #302 PR, then 64.0 -> 66.0 kB (66,000 B) gzip in the #303 PR** (2026-09-03): that fix measured 63,680 B against a 63,497 B baseline — 180 B over the old line. Raised rather than squeezed. Framed honestly: ADR-0004 non-negotiable 3 does NOT authorise this — it says the bundle "must not regress past the budget", and its delegation clause is scoped to "the PR that adds each frontend chunk", whereas #302 adds no chunk (it grows the eager one) and sits outside the ADR-0004 sequence those non-negotiables are headed by. So this is a deliberate decision recorded here for the owner to ratify, not a permission the ADR already granted. **As of 2026-09-03 this number is ENFORCED**, at the owner's direction: it lives in `frontend/bundle-budget.json` and `npm run check:bundle` runs in the `frontend` workflow. Before that it was prose in this row — raised and recorded by the same person, with nothing checking in either direction, and it moved twice in two PRs. The script REBUILDS the shipping variant itself rather than measuring whatever is in `dist/`: a first version tried to DETECT the mode from the built output and silently never fired, because Vite constant-folds `VITE_API_LIVE` away entirely — it only looked like it worked because the demo build is ~860 B larger and tripped the size check instead. Proven both ways: lowering the budget fails, and a demo build sitting in `dist/` no longer changes the measurement. #303 then added the markdown-subset renderer and citation chips: **65,329 B (+1,513 B over #302's 63,816 B)**. That delta is the whole argument for hand-rolling the parser rather than importing one — a markdown library costs 8-12 kB, so this is 6-9x cheaper — but it still needed the second raise. Current line **66,000 B, 671 B headroom**. Measure with `frontend/.env.local` present (`VITE_API_LIVE=true`) — a bare `git archive` checkout lacks it and builds ~860 B LARGER (measured: 66,089 B vs 65,229 B on the same tree), which is not what ships. See `docs/ADR/0004-user-accounts.md` |

## Operator / non-code follow-ups (not GitHub issues)

- **#227 (Node 22 → 26 base image) merged deliberately, all three pins moved
  together:** `frontend/.nvmrc`, `frontend/package.json` `engines.node`, and
  `infra/docker/Dockerfile.api:55`'s `FROM node:26-bookworm-slim`, exactly as
  `backend/tests/test_node_version_pin.py` requires. Boot-verified before
  merge, not assumed: `make image-smoke` built the frontend stage under Node
  26 (`npm ci` + `npm run build`), and both the api image (`GET /health` →
  200) and the worker image (`list-sources` exit 0) booted clean. Full
  backend battery (1437/17, lint, pyright) stayed green. This corroborates
  the #231 measurement already on record — Node 20/22/26 produce a
  byte-identical frontend bundle — so the base-image jump carries no known
  runtime risk, unlike the #34 Python 3.14 base-image bump, which shipped
  non-booting because nothing boot-tested it.

- **v0.12.0 release-candidate pack ready:** `docs/RELEASE_CANDIDATE_v0.12.0.md`
  (this session, 2026-08-31) has the ship/no-ship summary and exact ordered
  commands for the owner to tag + `fly deploy`. No migration and no corpus
  change since `v0.11.0`; `answer_cache` needs no flush (policy-version bump
  makes stale rows unreachable by construction, verified from
  `build_cache_key`). Tagging and deploying are owner-gated actions this
  session does not take.

- **Enable the CI answer-quality gate (owner-only):** add `CITEVYN_OPENROUTER_API_KEY` as a
  repo Actions secret (*Settings → Secrets and variables → Actions*). The `answer-quality-eval`
  CI job (PR #127) skips until it is present; adding it flips the judged MIN_MEAN_JUDGE +
  groundedness + injection gate on. Config proven locally (`--postgres`,
  `openai/text-embedding-3-small`, `router`, `CITEVYN_EVAL_JUDGE_PANEL=1`). Recommend one
  trial PR run before making it a *required* check. See `docs/AUTORUN_STATUS.md` (top block).
- **Live semantic e2e for #51:** set `CITEVYN_EMBEDDING_PROVIDER=gemini` + `CITEVYN_GEMINI_API_KEY`,
  re-ingest, and confirm a landing-page question returns a substantive, correctly-cited
  answer. The plumbing is verified end-to-end; only real-key semantic quality remains.
  See RUNBOOK §3.4a.

## Design references

- `docs/ADR/0003-embeddings-provider.md` — embedding provider decision, rejected
  alternatives, and the full "Deferred / Future Work" list these issues are drawn from.
